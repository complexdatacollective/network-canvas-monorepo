// Filled by the webhook slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const WEBHOOK_TABLES = {};
export const WEBHOOK_SIDECAR_SQL = '';
