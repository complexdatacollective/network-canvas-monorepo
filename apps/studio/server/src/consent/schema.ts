// Filled by the consent slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const CONSENT_TABLES = {};
export const CONSENT_SIDECAR_SQL = '';
