// Filled by the network slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const NETWORK_TABLES = {};
export const NETWORK_SIDECAR_SQL = '';
