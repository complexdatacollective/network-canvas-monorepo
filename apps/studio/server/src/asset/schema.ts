// Filled by the asset slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const ASSET_TABLES = {};
export const ASSET_SIDECAR_SQL = '';
