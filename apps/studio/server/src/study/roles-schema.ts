// Filled by the study-roles slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const STUDY_ROLE_TABLES = {};
export const STUDY_ROLE_SIDECAR_SQL = '';
