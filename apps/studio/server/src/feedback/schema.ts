// Filled by the feedback slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const FEEDBACK_TABLES = {};
export const FEEDBACK_SIDECAR_SQL = '';
