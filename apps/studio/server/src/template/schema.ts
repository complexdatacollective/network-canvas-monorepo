// Filled by the template slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const TEMPLATE_TABLES = {};
export const TEMPLATE_SIDECAR_SQL = '';
