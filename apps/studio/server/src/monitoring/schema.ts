// Filled by the monitoring slice of the Studio data-model work; registered in
// db/schema.ts ahead of its tables so the registration is settled once.
export const MONITORING_TABLES = {};
export const MONITORING_SIDECAR_SQL = '';
