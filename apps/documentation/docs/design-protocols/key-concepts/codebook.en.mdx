---
title: Codebook
---

<Definition>
  The codebook is a centralized view in Architect that displays all entity types
  and variables defined in your protocol, helping you maintain consistency and
  plan your data analysis.
</Definition>

## Details

The protocol's codebook provides an overview of all entities and variables defined in your protocol. This centralized view helps you ensure consistency across stages and makes it easier to plan your data analysis before deploying your interview.

Architect automatically constructs the codebook as you create variables in your protocol. How you open it depends on which version of Architect you use:

- In **Architect Desktop**, click the **Manage codebook** button at the top of a protocol file.
- In **Architect Web**, open the **Codebook** page from the navigation bar at the top of the editor.

![Clicking the Manage codebook button in Architect Desktop navigates to the codebook view](/assets/img/key-concepts/codebook/manage-codebook.png)

![In Architect Web, the codebook is a dedicated page opened from the Codebook link in the top navigation bar](/assets/img/architect-web/codebook-web.png)

## Entity types

The codebook organizes entities by entity type -- ego, node, and edge, and organizes variables by entity. You can edit node and edge types from the codebook using the **Edit Entity** button. Each node and edge type lists the stages where they are used, and you can navigate to those stages by clicking on one of the "used in" stage names.

![A view of the codebook in Architect](/assets/img/key-concepts/codebook/codebook.png)

In both versions of Architect you can customize an entity type's name and color. Architect Web goes further for node types, letting you also set the shape and icon used to represent the node across interfaces.

![Editing a node type in Architect Web, where you can set its shape and icon in addition to its name and color](/assets/img/architect-web/edit-node-type-web.png)

In Architect Web you can also make a node type's shape **dynamic**, so that the shape reflects the value of one of the node's variables rather than staying fixed. Enable the **Map variable to shape** option in the node type's Shape section to override the default shape based on a variable's value. The mapping works in one of two ways depending on the variable you choose:

- For **categorical**, **ordinal**, or **boolean** variables, you assign a shape to each discrete value. Any value you leave unmapped falls back to the default shape.
- For **number** or **scalar** variables, you define up to two numeric **thresholds**. Values below the first threshold use the default shape, and each threshold sets the shape used at or above its value.

The shapes available are the same as for the default shape: circle, square, and diamond.

## Variables

Each variable lists the name, type, [input control](/en/design-protocols/key-concepts/input-controls), and where it is used. You can navigate to any of the stages a variable is used in by clicking on one of the "used in" stage names just like the entity types.

![Entity types and variables both indicate which stages they are used in.](/assets/img/key-concepts/codebook/used-in.png)

If a variable is defined but later becomes obsolete, you can see this as an unused variable within the codebook, marked **not in use**. Only variables that are unused can be deleted from the codebook. Entity types can also only be deleted if they are unused in the protocol.

![Variables marked 'not in use' and unused entity types can be deleted in the codebook view.](/assets/img/key-concepts/codebook/unused-variables.png)

## Network assets

Codebooks also contain a summary of network assets when [external network data](/en/design-protocols/key-concepts/resources/#network) has been included in a protocol. The heading row of network data will be used as the variable names, and the codebook lists these variable names as a summary for each external network asset.

![A summary of the variables from an external network file.](/assets/img/key-concepts/codebook/network-data.png)

## Related concepts

- [Variables](/en/design-protocols/key-concepts/variables) - Understanding variable types and configuration
- [Input Controls](/en/design-protocols/key-concepts/input-controls) - Choose appropriate controls for your variables
- [Resources](/en/design-protocols/key-concepts/resources) - Import external data files including network rosters
- [Forms](/en/design-protocols/key-concepts/forms) - Create forms using variables from your codebook
- [Interfaces](/en/design-protocols/key-concepts/interfaces) - Learn about Interfaces that use variables from the codebook
