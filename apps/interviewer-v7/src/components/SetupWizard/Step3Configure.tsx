// TODO (Task 20): Replace this stub with the real configure step content.
// This step commits vault enrolment — Step3Configure owns the vault write.
// On revisit (back navigation) the step should render read-only.
export default function Step3Configure() {
  return <div>Step 3 — Set up your method (TODO: Task 20)</div>;
}
