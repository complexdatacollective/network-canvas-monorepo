<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.allow-dyld-environment-variables</key>
	<false/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!--
	Required for keychain items with biometric access control
	(kSecAttrAccessControl + USER_PRESENCE). Without this entitlement,
	SecItemAdd returns errSecMissingEntitlement (-34018). The entitlement
	is restricted: AMFI will refuse to launch the binary unless the
	embedded provisioning profile (build-resources/embedded.provisionprofile,
	wired by electron-builder.config.cjs) authorizes the access group.
	The single entry below is the app's DEFAULT keychain access group
	(Team ID + bundle identifier).
	-->
	<key>keychain-access-groups</key>
	<array>
		<string>85EZ69PQHJ.Network-Canvas-Interviewer-8</string>
	</array>
</dict>
</plist>
