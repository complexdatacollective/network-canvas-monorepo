{
  "protocolValidation.contradiction.conflictingReferencePair": "The rules require {variables} to be both equal and different. Remove one of these conflicting rules.",
  "protocolValidation.contradiction.disjointBounds": "The comparisons for {variables} cannot be satisfied within their allowed ranges. Adjust the ranges, comparisons, or input controls.",
  "protocolValidation.contradiction.invertedBounds": "The minimum and maximum rules for {variables} leave no permitted answer. Adjust the bounds or the required-answer rule.",
  "protocolValidation.contradiction.minSelectedExceedsOptions": "The minimum selection for {variables} exceeds the available options. Add options or reduce the minimum.",
  "protocolValidation.contradiction.oddDifferentFromCycle": "The rules require different values for {variables}, but too few values are available. Add allowed values or remove a conflicting rule.",
  "protocolValidation.contradiction.pinnedDifferentFromParity": "The fixed values and rules requiring different answers for {variables} cannot all be satisfied. Change a fixed value or a comparison.",
  "protocolValidation.contradiction.pinnedEqualDifferentFrom": "The rules require different answers for {variables}, but their allowed ranges force the same value. Widen a range or change the comparison.",
  "protocolValidation.contradiction.sameAsGroupConflict": "The rules require {variables} to share a value, but their allowed values or date resolutions are incompatible. Align their rules and input controls.",
  "protocolValidation.contradiction.strictComparatorCycle": "The comparisons for {variables} form a cycle that no values can satisfy. Change a comparison to break the cycle.",
  "protocolValidation.file.cannotUpgrade": "This protocol was made with a version of Network Canvas it cannot be upgraded from. Open it in the version that made it and save it there first.",
  "protocolValidation.file.damagedJson": "This protocol's contents are damaged and cannot be read. Try a backup, or the copy you originally downloaded.",
  "protocolValidation.file.inflationLimit": "This protocol file expands to more data than can be opened safely. It may be corrupt or malicious.",
  "protocolValidation.file.invalidAsset": "One of this protocol's resources is described in a way this version does not understand, so the protocol cannot be opened.",
  "protocolValidation.file.invalidBeforeUpgrade": "This protocol did not pass the checks for the version it was made with, so it could not be upgraded.",
  "protocolValidation.file.missingAsset": "This protocol refers to a file that isn't included in it.",
  "protocolValidation.file.missingNamedAsset": "This protocol refers to a file that isn't included in it: \"{assetName}\".",
  "protocolValidation.file.missingProtocol": "This file is missing its protocol, so there is nothing to open. It may have been created by another program, or damaged in transit.",
  "protocolValidation.file.missingVersion": "This file does not say which version of Network Canvas made it, so it cannot be opened.",
  "protocolValidation.file.newerVersion": "This protocol was made with a newer version of Network Canvas. Update to the latest version to open it.",
  "protocolValidation.file.notArchive": "This file isn't a Network Canvas protocol. Check that you chose the right file, and that it finished downloading.",
  "protocolValidation.file.upgradeFailed": "This protocol could not be upgraded to the current version.",
  "protocolValidation.file.upgradeStepFailed": "This protocol could not be upgraded to the current version. Nothing has been changed on this device.",
  "protocolValidation.migrationNotes.schema5.interviewScript": "Enable an 'Interview Script' section for each stage, where notes for the interviewer can be added.",
  "protocolValidation.migrationNotes.schema5.referenceValidation": "Add new validation options for form fields: `unique`, `sameAs`, and `differentFrom`.",
  "protocolValidation.migrationNotes.schema5.tieStrengthCensus": "Enable the 'Tie Strength Census' interface, which will allow you to conduct a dyad census that also captures the strength of the tie and assigns it to an ordinal attribute.",
  "protocolValidation.migrationNotes.schema6.rosterNameGenerator": "Replace roster-based name generators (small and large) with a single new interface that combines the functionality of both. This will change the interview experience, and may impact your data collection!",
  "protocolValidation.migrationNotes.schema6.sociogramAutomaticLayout": "Enable support for using the automatic node positioning feature on the Sociogram interface.",
  "protocolValidation.migrationNotes.schema7.alterCountLimits": "Add the ability to specify minimum and maximum numbers of named alters on name generator stages.",
  "protocolValidation.migrationNotes.schema7.categoricalSkipLogic": "Add additional skip logic options for handling ordinal and categorical attributes.",
  "protocolValidation.migrationNotes.schema8.anonymisationInterface": "New experimental interface: \"anonymisation interface\". Allows the participant to encrypt sensitive/identifiable information, so that it cannot be read by the researcher. Not enabled by default. Contact the team for details.",
  "protocolValidation.migrationNotes.schema8.automaticLayoutBoolean": "The Sociogram and Narrative `automaticLayout` behaviour is now a plain boolean (previously `'{ enabled }'`); existing values are flattened. The Narrative interface gains this behaviour for the first time; it is only active when explicitly enabled, so existing Narrative stages keep their hand-authored static positions.",
  "protocolValidation.migrationNotes.schema8.categoricalArrays": "Categorical attribute values are now stored as arrays of selected option values. Existing single-value categorical filter and skip-logic rule operands (`is exactly`, `is not`, `includes`, `excludes`) are wrapped in a single-element array to match.",
  "protocolValidation.migrationNotes.schema8.comparisonValidation": "Add new validation options for form fields: `greaterThanVariable` and `lessThanVariable`.",
  "protocolValidation.migrationNotes.schema8.contradictoryRules": "Validation rules that contradict each other are removed so existing protocols stay valid under the new schema checks: inverted `min`/`max` pairs (both removed), `required` text or categorical attributes whose maximum is zero (the zero maximum is removed), `minSelected` above the option count, `sameAs` and `differentFrom` naming one target (both removed), comparator structures no value can satisfy — impossible cycles, comparisons inside a `sameAs` group, comparisons whose value ranges cannot overlap (the comparator is removed; value bounds are kept), `sameAs` groups whose bounds share no value (the `sameAs` rules are removed) — and validation references to an attribute of a different type. Count-valued rules must be non-negative; negative values are removed.",
  "protocolValidation.migrationNotes.schema8.datePickerBounds": "DatePicker `min`/`max` parameters must be real dates written exactly at the picker's resolution, with `min` not after `max`. Values with more precision than the resolution are truncated; other invalid values are removed. At year or month resolution, a bound must use a four-digit year of 1000 or later — the interview builds that resolution's year options unpadded, so an earlier, zero-padded year could never match a stored value; such a bound is removed. Any parameter key other than `type`, `min`, or `max` — e.g. a RelativeDatePicker `anchor` left over from a component switch — is also removed.",
  "protocolValidation.migrationNotes.schema8.datetimeParameterObject": "A datetime attribute's `parameters` must be a plain object; a wrong-typed value (a string, number, list, or null) is removed, reverting the picker to its defaults.",
  "protocolValidation.migrationNotes.schema8.duplicateFormAttributes": "A form may no longer collect the same attribute twice. Two fields naming one attribute always shared a single answer — whichever the participant filled in last overwrote the other — so the repeat was never collecting anything of its own. Only the first field for each attribute is kept.",
  "protocolValidation.migrationNotes.schema8.emptyBooleanOptions": "A boolean attribute's `options` must now offer at least one choice. An empty list rendered a control with no buttons at all, which a participant could never answer; the empty list is removed so the attribute falls back to the standard Yes/No choices.",
  "protocolValidation.migrationNotes.schema8.familyPedigree": "New interface: \"family pedigree\". A pedigree building interface designed for genetic disease monitoring scenarios, with configurable node and edge types, relationship attributes, and optional disease/condition nomination prompts.",
  "protocolValidation.migrationNotes.schema8.fieldHints": "Added optional 'hint' property to form fields, allowing a markdown string to be displayed as additional guidance for participants.",
  "protocolValidation.migrationNotes.schema8.fieldInputControl": "A codebook attribute referenced by a form field must define a `component` (input control). Previously this was only checked by the Architect editor; a protocol violating it crashed the interview when the form rendered.",
  "protocolValidation.migrationNotes.schema8.filterRuleEntities": "Changed FilterRule type to use the same entity names as elsewhere",
  "protocolValidation.migrationNotes.schema8.geospatialInterface": "New interface: \"geospatial interface\". Allows the participant to select a location on a map based on a geojson shapefile.",
  "protocolValidation.migrationNotes.schema8.informationTitle": "The Information stage `title` (page heading) is now required. Any Information stage without one is given its stage label as the title, or \"Information\" when no label was authored.",
  "protocolValidation.migrationNotes.schema8.inputControlCompatibility": "An attribute's `component` (input control) must be one its type can render. An unrecognised or mismatched control is replaced with the type's standard control (for datetime, chosen by the shape of its `parameters`); layout attributes, which have no control, have it removed.",
  "protocolValidation.migrationNotes.schema8.membershipComparators": "Add new comparator options for skip logic and filter: `contains` and `does not contain`.",
  "protocolValidation.migrationNotes.schema8.multiValueMembership": "Amplify comparator options `includes` and `excludes` for ordinal and categorical attributes to allow multiple selections.",
  "protocolValidation.migrationNotes.schema8.nameGeneratorTitle": "The NameGenerator `form.title` (heading of the add-a-person dialog) is now required. Any NameGenerator form without one is given \"Add '{node type name}'\" (e.g. \"Add Person\").",
  "protocolValidation.migrationNotes.schema8.nodeIcon": "Renamed 'iconVariant' to 'icon' on node definitions.",
  "protocolValidation.migrationNotes.schema8.nodePalette": "A node type's `color` is now restricted to the eight `node-color-seq-1`–`node-color-seq-8` palette values the interfaces can render. Older versions of Architect offered ten, so a node type using the ninth or tenth is moved back onto the palette (the ninth becomes the first, the tenth the second).",
  "protocolValidation.migrationNotes.schema8.nodeShape": "Added 'shape' property with default 'circle' to all node definitions.",
  "protocolValidation.migrationNotes.schema8.oneToManyDyadCensus": "New interface: \"one-to-many dyad-census\". Allows the participant to link multiple alters at a time.",
  "protocolValidation.migrationNotes.schema8.optionValueTypes": "Ordinal and categorical option values must be strings or whole numbers; a fractional value is converted to its string form (as legacy boolean values already are), and a numeric option label becomes the same text it already displayed. A boolean attribute's option entry that is not a labelled true/false choice is removed; if no entries remain the attribute falls back to the standard Yes/No choices.",
  "protocolValidation.migrationNotes.schema8.ordinalPalette": "An OrdinalBin prompt `color` is now required, restricted to the ten `ord-color-seq-1`–`ord-color-seq-10` palette values the interface can render. Any other value was silently ignored and is removed; prompts without a valid color default to the first palette color (`ord-color-seq-1`), the runtime's previous fallback.",
  "protocolValidation.migrationNotes.schema8.orphanOtherConfig": "A CategoricalBin prompt `otherOptionLabel` or `otherVariablePrompt` without an accompanying `otherVariable` was silently ignored, as was an empty-string `otherVariable`. Such orphaned properties are removed.",
  "protocolValidation.migrationNotes.schema8.otherAndQuickAddValidation": "The CategoricalBin \"other\" input and the NameGenerator quick-add field now honour the referenced attribute's configured validation. Both previously required a response locally, so migration adds `required: true` to every attribute they reference while preserving its other validation rules.",
  "protocolValidation.migrationNotes.schema8.otherLabels": "A CategoricalBin prompt with `otherVariable` set now requires both `otherVariablePrompt` and `otherOptionLabel` (previously a missing label silently dropped the whole \"other\" bin). A missing value is backfilled from the other authored one, else \"Please specify\" / \"Other\".",
  "protocolValidation.migrationNotes.schema8.otherTextAttribute": "A CategoricalBin prompt's `otherVariable` must reference a text attribute because its follow-up control records text. A non-text reference and its associated \"other\" configuration are removed.",
  "protocolValidation.migrationNotes.schema8.protocolName": "Added 'name' property to protocol (required dependency for migration)",
  "protocolValidation.migrationNotes.schema8.relativeDateParameters": "A datetime codebook attribute's RelativeDatePicker `anchor` must be a real date inside the native input's year range of 0001–9999, and its `before`/`after` offsets must be non-negative whole numbers of days. Invalid values, and any unrecognised parameter, are removed; a removed anchor reverts the picker to its interview-date default.",
  "protocolValidation.migrationNotes.schema8.removeDisplayVariable": "Removed 'displayVariable' property, if set. This property was not used, and has been marked as deprecated for a long time.",
  "protocolValidation.migrationNotes.schema8.removeMediaLoop": "Removed 'loop' property from Information stage items and video/audio assets. This property was never honoured by Interviewer.",
  "protocolValidation.migrationNotes.schema8.removeScalarBounds": "Removed the `minValue` and `maxValue` validators from scalar (visual analog scale) attributes. A scalar response is recorded on a normalised 0-1 scale, so a value bound on it was never meaningful. Any such validator is removed.",
  "protocolValidation.migrationNotes.schema8.removeToggleOptions": "Removed 'options' property for boolean Toggle attributes. This property was not used.",
  "protocolValidation.migrationNotes.schema8.requiredBackground": "The Sociogram and Narrative `background` is now required and must be exactly one of its two variants: an image (`image` set, no `concentricCircles`) or concentric circles (`concentricCircles` set to a whole number, no `image`; 0 renders no rings). Stages with no background, or with an incomplete or contradictory one, are normalised: an image wins when present; otherwise `concentricCircles` defaults to 4, matching what the interview already rendered.",
  "protocolValidation.migrationNotes.schema8.requiredBounds": "A `minValue`, `minLength`, or `minSelected` validator no longer implies a field is required. To preserve the effective behaviour of existing protocols that relied on this coupling, any codebook attribute (node, edge, or ego) with one of these validators and no explicit `required: true` now has `required: true` set.",
  "protocolValidation.migrationNotes.schema8.requiredTextFields": "Several free-text fields that the Architect editor already requires are now required (non-empty) in the schema: a prompt's `text`, a form field's `prompt`, an introduction panel's `title` and `text`, an Information item's `content`, a Narrative preset's `label`, a side panel's `title`, a NameGeneratorRoster `dataSource`, and its `searchOptions.matchProperties` (at least one). Any that were empty are backfilled — the form-field prompt from the attribute's name, the panel title from the stage label, a preset/side-panel label by position — else a plain default. An empty `searchOptions`, and an Information asset item with no asset id (a broken reference), are dropped. (The FamilyPedigree `censusPrompt`, NarrativePedigree disease `label`/`color`, and Anonymisation `explanationText` are likewise required but are v8-only, so no migration is needed.)",
  "protocolValidation.migrationNotes.schema8.skipDestinations": "Add optional targeted skip-logic destinations. When a stage is hidden, routing can continue at the next available stage, jump to a later stage, or continue to the interview finish screen.",
  "protocolValidation.migrationNotes.schema8.sociogramHighlightAndEdges": "A Sociogram prompt with `highlight.allowHighlighting` enabled must name the boolean attribute to toggle, and an `edges` object must set `create` and/or `display`. Prompts violating either were runtime no-ops; the highlight toggle is turned off and the empty edges object removed.",
  "protocolValidation.migrationNotes.schema8.stageLabels": "Stage labels are now required to be non-empty. Any stage with a missing or empty label is given a default name based on its position (e.g. \"Stage 3\").",
  "protocolValidation.migrationNotes.schema8.unsupportedValidation": "Validation rules the new schema cannot express are removed: rule names it has never defined, rules whose value has the wrong type (e.g. a quoted number), and rules that do not apply to the attribute's type (e.g. `minValue` on a text attribute, or `requiredAcceptsNull` anywhere). A removed `minValue`/`minLength`/`minSelected` still marks the attribute required, preserving the old implied-required behaviour. Layout attributes take no validation at all; theirs is removed.",
  "protocolValidation.migrationNotes.schema8.validationHints": "Added optional 'showValidationHints' property to form fields, enabling automatic display of hints derived from validation rules.",
  "protocolValidation.rule.additionalAttributes": "Fixed attributes",
  "protocolValidation.rule.component": "Input control",
  "protocolValidation.rule.differentFrom": "Different from another attribute",
  "protocolValidation.rule.edgeConfig": "Link configuration",
  "protocolValidation.rule.egoVariable": "Participant indicator",
  "protocolValidation.rule.greaterThanOrEqualToVariable": "Greater than or equal to another attribute",
  "protocolValidation.rule.greaterThanVariable": "Greater than another attribute",
  "protocolValidation.rule.inheritancePattern": "Inheritance pattern",
  "protocolValidation.rule.lessThanOrEqualToVariable": "Less than or equal to another attribute",
  "protocolValidation.rule.lessThanVariable": "Less than another attribute",
  "protocolValidation.rule.maxLength": "Maximum text length",
  "protocolValidation.rule.maxSelected": "Maximum selection",
  "protocolValidation.rule.maxValue": "Maximum value",
  "protocolValidation.rule.minLength": "Minimum text length",
  "protocolValidation.rule.minSelected": "Minimum selection",
  "protocolValidation.rule.minValue": "Minimum value",
  "protocolValidation.rule.parameters": "Input control settings",
  "protocolValidation.rule.required": "Required answer",
  "protocolValidation.rule.requiredAcceptsNull": "Allow explicit missing answers",
  "protocolValidation.rule.sameAs": "Same as another attribute",
  "protocolValidation.rule.unique": "Unique value"
}
