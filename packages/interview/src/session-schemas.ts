// Re-exposed at the package boundary; the implementation lives in
// store/modules/session.ts. `StageMetadataSchema` now lives in
// `@codaco/shared-consts` — import it directly from there.
export { createInitialNetwork } from './store/modules/session';
