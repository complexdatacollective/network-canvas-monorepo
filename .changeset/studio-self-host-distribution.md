---
'@codaco/studio-server': minor
---

Ship the self-host deployment bundle with private PostgreSQL and object storage, HTTPS, explicit configuration and read-only diagnostics. Add quiesced recovery with independent encryption-key custody, refuse existing restore targets before replacing local configuration, and preserve verified hashed browser assets through updates and recovery. Qualify the built image against populated recovery and aggregate performance workloads.

Add an installer that verifies the selected release manifest and every image, preserves replay protection across failed updates, and holds one operation lock while it drains services, captures a backup, migrates and checks authenticated access before reopening admission.

Gate distribution publication on the exact reviewed source, released dependencies and reserved ancestry. Bind both image platforms, per-image CycloneDX evidence and the immutable MinIO source archive into the signed manifest; reconcile immutable draft assets and verify every retained byte before publication.
