---
"@codaco/fresco-ui": minor
---

SegmentedToolbar: menu segments can now be action menus (plain menu items with
correct `menuitem` semantics for fire-and-forget commands, alongside the
existing single-select radio menus), and popover segments support controlled
`open`/`onOpenChange`, `defaultOpen`, and a `disabled` trigger state.
