---
"@codaco/fresco-ui": minor
---

Internal: switch CSS bundle to the new `@codaco/tailwind-config/fresco.css` barrel introduced in tailwind-config 0.5.0. No consumer-facing changes — `@codaco/fresco-ui/styles.css` still emits the same effective CSS. Requires `@codaco/tailwind-config@^0.5.0` (peer).
