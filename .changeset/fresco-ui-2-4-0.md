---
"@codaco/fresco-ui": minor
---

Update peer dependency on `@codaco/tailwind-config` to track its 1.0 alpha. No source changes — `dist/styles.css` already imports `@codaco/tailwind-config/fresco.css`, which is the public path that survived the tailwind-config reorganization.
