---
'@codaco/interview': patch
---

Sociogram prompts with a display-only highlight (a `highlight.variable` with `allowHighlighting` off) render matching nodes highlighted; tapping a node only toggles the highlight attribute when `allowHighlighting` is enabled.
