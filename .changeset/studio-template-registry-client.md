---
'@codaco/studio-sync': minor
---

Add a bounded Template Registry client and shared public wire contract for verified metadata, artifact downloads, and explicit-credential publication.

Validate publication metadata against the artifact's committed template, metadata and license fields, and expose the same comparison for verified imports.
