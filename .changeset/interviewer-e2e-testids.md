---
'@codaco/interviewer': patch
---

Add data-testid hooks to app chrome (protocol deck import, DataView toolbar/resume, TopActionBar settings trigger, SettingsDialog synthetic controls, NewSessionForm, InterviewComplete, LockScreen/UnlockForms, App.tsx background lights) to support the new end-to-end test suite. No user-facing behaviour change.
