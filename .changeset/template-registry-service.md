---
'@codaco/template-registry': minor
---

Add an independent template registry with verified publisher accounts, portable content-validated publications, public artifact retrieval, withdrawal notices, reporting and operator moderation. Include bounded service operation and separate migration, operator and backup verification entry points.

Require complete database login enrollment at startup, readiness and offline verification, preserving separate app/operator roles and explicit non-owner migration administrators. Check evidence permissions and every enrolled restricted identity before trusting schema state. Keep backup quarantine intact while checking its same verified connection.
