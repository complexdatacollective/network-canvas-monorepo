---
'fresco': patch
---

Fresco normalizes older stored and synchronized sessions at its read boundaries, so interviews containing nullish entity attributes continue to hydrate and synchronize. Malformed synchronization JSON now returns a controlled bad-request response.
