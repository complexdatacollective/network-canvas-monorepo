---
'@codaco/studio-server': minor
---

Encrypt participant contact fields and OAuth/webhook credentials with versioned keys, require immutable audits for protected reads, and add durable key verification and resumable maintenance rotation.
