---
'@codaco/interview': patch
---

Sociogram: when automatic layout is paused, adding or removing an edge no longer restarts the layout. It now stays paused until the user resumes it with the toggle.
