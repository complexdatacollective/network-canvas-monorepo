---
'@codaco/fresco-ui': minor
---

Add an `appearance` prop (`solid` | `soft`) to `Alert`. `solid` (the default, unchanged) fills the alert with its intent colour; `soft` renders a low tint over the surface with surface text and an intent-coloured link, for quieter content-adjacent notices. Role, aria-live, screen-reader label and icon are identical across appearances.
