---
'@codaco/fresco-ui': patch
---

Add data-testid hooks to SegmentedCodeField (`segmented-code-${name}` on the fieldset) and Toast.Viewport (`toast-viewport`) to support locators in the Interviewer end-to-end test suite. No user-facing behaviour change.
