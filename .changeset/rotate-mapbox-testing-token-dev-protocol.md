---
'@codaco/development-protocol': patch
---

Rotate the shared Mapbox testing token carried by the development protocol's Geospatial stage. The new token is URL-restricted to networkcanvas.com, networkcanvas.dev and localhost, so the map only renders on those hosts.
