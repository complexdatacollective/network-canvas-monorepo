---
"@codaco/studio-sync": minor
---

Add a shared offline recovery quarantine check that binds the recovery and backup connections to one database and observes current login permissions without a stale transaction snapshot.
