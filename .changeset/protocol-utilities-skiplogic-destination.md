---
'@codaco/protocol-utilities': patch
---

Support the skipLogic `destination` field (targeted skip-to-stage / skip-to-finish) on `SkipLogicInput`, so synthetic protocols built with `SyntheticInterview` can exercise targeted skip routing.
