---
'networkcanvas.com': patch
---

The protocol gallery facet chips, the Get Started path and app-status labels,
the Summer Update compatibility chips and the news ticker label all use the
shared `Badge`, so they share one size, weight and theme-aware colour
treatment instead of hand-rolled approximations.
