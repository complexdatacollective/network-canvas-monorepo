---
'@codaco/interview': patch
---

Fix `ActionButton` icons with asymmetric glyph geometry (e.g. the rotate/refresh icons) rendering off-centre — Lucide icons are now constrained to a fixed square so any icon centres reliably. Improve the visibility of the "missing" bin on the Ordinal Bin interface, which previously blended into the background; it now uses visible neutral surface tokens.
