---
'@codaco/studio-server': minor
'@codaco/template-registry': minor
---

Install the independent Template Registry from the authenticated self-host bundle with its own domain, mail, database and object-store credentials. Quiesce and retain both products during upgrades, and restore them into unused resources while public admission and workers remain closed.

Require independently held Registry configuration and current hash-pinned account reconciliation for populated recovery. Invalidate restored sessions, magic links and personal access tokens, reconcile publisher/operator state, and verify every referenced artifact before recovery can complete.
