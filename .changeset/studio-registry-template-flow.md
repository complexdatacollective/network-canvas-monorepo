---
'@codaco/studio-client': minor
'@codaco/studio-server': minor
'@codaco/studio-rpc': minor
'@codaco/studio-sync': minor
---

Link a personal Template Registry publisher identity, publish immutable local template versions with an explicit Registry credential, and import verified Registry entries with durable origin records.

Preserve imported asset aliases across content deduplication, reconcile only against the recorded Registry, and show background completion or quarantine without retaining publishing credentials.

Quarantine disabled Registry work and permanently unavailable imports, and return published template timestamps through the RPC date contract.
