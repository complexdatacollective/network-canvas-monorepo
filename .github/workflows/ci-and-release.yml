name: CI and Release

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Force all conditional jobs (deploys, chromatic, e2e) to run regardless of turbo-ignore detection.'
        type: boolean
        default: false

permissions:
  contents: read
  packages: read
  actions: read
  checks: write
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      any_app: ${{ steps.flags.outputs.any_app }}
      docs: ${{ steps.flags.outputs.docs }}
      architect: ${{ steps.flags.outputs.architect }}
      fresco_ui_storybook: ${{ steps.flags.outputs.fresco_ui_storybook }}
      interview_storybook: ${{ steps.flags.outputs.interview_storybook }}
      interview_e2e: ${{ steps.flags.outputs.interview_e2e }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
      - id: flags
        env:
          # Manual runs may set force_run=true to bypass detection and run
          # every conditional job (replaces the old per-workflow rerun buttons).
          FORCE_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.force_run == true }}
        run: |
          # Determine the comparison base — the commit immediately before the
          # one we're checking. We DON'T use turbo-ignore because it includes
          # global config (turbo.json, lockfile, root configs) in its hash, so
          # any unrelated workflow tweak invalidates every package's hash.
          #
          # For PRs we want push-to-push semantics: only RE-RUN jobs whose
          # packages changed between the prior PR commit and the new tip. The
          # first push on a PR is a degenerate case — HEAD^2^1 resolves to
          # the base branch HEAD, so the push diff IS the cumulative PR diff
          # at that point and every relevant job fires at least once.
          # Subsequent pushes only re-trigger impacted jobs; jobs that
          # already ran on earlier PR commits keep their prior status (see
          # the carry-forward job below) instead of being re-executed.
          #
          # GH Actions checks out a synthetic merge commit on PRs; HEAD^2 is
          # the actual PR tip, HEAD^2^1 is the prior commit on the source
          # branch (or the base branch HEAD on the first PR push).
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            CURR=$(git rev-parse HEAD^2 2>/dev/null || git rev-parse HEAD)
            PREV=$(git rev-parse "${CURR}^1" 2>/dev/null || true)
          else
            CURR=$(git rev-parse HEAD)
            PREV=$(git rev-parse HEAD^1 2>/dev/null || true)
          fi
          echo "comparing $PREV..$CURR"

          # flag <package>: true if any file in the package's workspace dep
          # tree changed between PREV and CURR, false otherwise.
          flag() {
            local pkg=$1
            if [[ "$FORCE_RUN" == "true" ]]; then
              echo true; return
            fi
            # First commit on a branch (no parent reachable) — be conservative
            # and run everything.
            if [[ -z "$PREV" ]]; then
              echo true; return
            fi
            local paths
            paths=$(npx --yes turbo@2.9.6 ls --filter="${pkg}..." --output=json 2>/dev/null \
              | jq -r '.packages.items[].path')
            local pathspec=()
            while IFS= read -r p; do
              [[ -n "$p" ]] && pathspec+=("$p")
            done <<<"$paths"
            if git diff --quiet "$PREV" "$CURR" -- "${pathspec[@]}"; then
              echo false
            else
              echo true
            fi
          }
          docs=$(flag @codaco/documentation)
          arch=$(flag @codaco/architect-web)
          fui=$(flag @codaco/fresco-ui)
          intv=$(flag @codaco/interview)
          any_app=false
          if [[ "$docs" == "true" || "$arch" == "true" ]]; then any_app=true; fi
          {
            echo "any_app=$any_app"
            echo "docs=$docs"
            echo "architect=$arch"
            echo "fresco_ui_storybook=$fui"
            echo "interview_storybook=$intv"
            echo "interview_e2e=$intv"
          } >> "$GITHUB_OUTPUT"

  # Cache design: every Turbo-using job restores `.turbo` from the same
  # `${{ runner.os }}-turbo-*` keyspace. Whichever job finishes its build first
  # writes the SHA-specific slot; subsequent jobs read it via `restore-keys`
  # fallback. Per-package env values (NEXT_PUBLIC_*, VITE_*) are part of each
  # build task's `env:` list in turbo.json so they participate in the cache key.
  quality:
    runs-on: ubuntu-latest
    # No NEXT_PUBLIC_* / VITE_* env vars here. Quality just verifies the build
    # compiles — env.js has skipValidation:true so missing public env values
    # don't fail the build. Keeping these vars unset in quality matches what
    # chromatic/e2e have, so shared package builds (fresco-ui, shared-consts,
    # interview, …) compute the same task hash across all those jobs and the
    # cache that quality writes is actually reusable downstream.
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - name: Write protocol-validation .env (test fixture)
        env:
          PROTOCOL_ENCRYPTION_KEY: ${{ secrets.PROTOCOL_ENCRYPTION_KEY }}
          PROTOCOL_ENCRYPTION_IV: ${{ secrets.PROTOCOL_ENCRYPTION_IV }}
          TEST_PROTOCOL_TOKEN: ${{ secrets.TEST_PROTOCOL_TOKEN }}
        run: |
          {
            echo "PROTOCOL_ENCRYPTION_KEY=$PROTOCOL_ENCRYPTION_KEY"
            echo "PROTOCOL_ENCRYPTION_IV=$PROTOCOL_ENCRYPTION_IV"
            echo "GITHUB_TOKEN=$TEST_PROTOCOL_TOKEN"
          } > packages/protocol-validation/.env
      - name: Run all gates
        run: |
          pnpm exec turbo run //#lint //#knip typecheck build test \
            --filter='!network-canvas-interviewer' \
            --filter='!network-canvas-architect'

  # Deploy jobs are split per-app and per-environment so:
  #   * one app's build failure never blocks the other app's deploy
  #   * preview deploys never wait on quality / chromatic / e2e
  #   * production deploys are strictly gated on every check passing
  #
  # netlify-cli flags:
  #   --no-build:    skip dashboard build command + plugin lifecycle hooks
  #   --filter=<pkg>: workspace selector (required because pnpm-workspace.yaml
  #                   is at the repo root and netlify-cli detects the monorepo)
  #   --dir:         pre-built directory to upload

  deploy-docs-preview:
    # needs:quality (not just detect) so quality finishes first and owns the
    # .turbo cache slot; deploy reads it back instead of racing. The
    # `always()` and `result != 'cancelled'` clauses mean a quality failure
    # doesn't block the preview deploy.
    needs: [detect, quality]
    if: |
      always() &&
      github.event_name == 'pull_request' &&
      needs.detect.outputs.docs == 'true' &&
      needs.quality.result != 'cancelled'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    env:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NEXT_PUBLIC_ALGOLIA_APPLICATION_ID: ${{ secrets.NEXT_PUBLIC_ALGOLIA_APPLICATION_ID }}
      NEXT_PUBLIC_ALGOLIA_INDEX_NAME: ${{ secrets.NEXT_PUBLIC_ALGOLIA_INDEX_NAME }}
      NEXT_PUBLIC_ALGOLIA_API_KEY: ${{ secrets.NEXT_PUBLIC_ALGOLIA_API_KEY }}
      NEXT_PUBLIC_MENDABLE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_MENDABLE_ANON_KEY }}
      NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - id: deploy
        name: Build and deploy docs preview
        run: |
          pnpm exec turbo run build --filter=@codaco/documentation
          cp apps/documentation/netlify.toml apps/documentation/out/netlify.toml
          json=$(npx --yes netlify-cli@22 deploy --no-build \
                   --filter=@codaco/documentation \
                   --dir=apps/documentation/out \
                   --site="${{ secrets.NETLIFY_SITE_ID_DOCS }}" \
                   --alias="pr-${{ github.event.number }}" --json)
          echo "$json" | jq .
          url=$(echo "$json" | jq -r '.deploy_url')
          echo "url=$url" >> "$GITHUB_OUTPUT"

  deploy-docs-prod:
    needs:
      [
        detect,
        quality,
        chromatic-fresco-ui,
        chromatic-interview,
        interview-e2e-report,
      ]
    if: |
      always() &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.detect.outputs.docs == 'true' &&
      needs.quality.result == 'success' &&
      needs.chromatic-fresco-ui.result != 'failure' &&
      needs.chromatic-interview.result != 'failure' &&
      needs.interview-e2e-report.result != 'failure'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    env:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NEXT_PUBLIC_ALGOLIA_APPLICATION_ID: ${{ secrets.NEXT_PUBLIC_ALGOLIA_APPLICATION_ID }}
      NEXT_PUBLIC_ALGOLIA_INDEX_NAME: ${{ secrets.NEXT_PUBLIC_ALGOLIA_INDEX_NAME }}
      NEXT_PUBLIC_ALGOLIA_API_KEY: ${{ secrets.NEXT_PUBLIC_ALGOLIA_API_KEY }}
      NEXT_PUBLIC_MENDABLE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_MENDABLE_ANON_KEY }}
      NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - id: deploy
        name: Build and deploy docs production
        run: |
          pnpm exec turbo run build --filter=@codaco/documentation
          cp apps/documentation/netlify.toml apps/documentation/out/netlify.toml
          json=$(npx --yes netlify-cli@22 deploy --no-build --prod \
                   --filter=@codaco/documentation \
                   --dir=apps/documentation/out \
                   --site="${{ secrets.NETLIFY_SITE_ID_DOCS }}" --json)
          echo "$json" | jq .
          url=$(echo "$json" | jq -r '.deploy_url')
          echo "url=$url" >> "$GITHUB_OUTPUT"

  deploy-architect-preview:
    # Same shape as deploy-docs-preview: wait for quality (cache + write-slot)
    # but don't gate on its success.
    needs: [detect, quality]
    if: |
      always() &&
      github.event_name == 'pull_request' &&
      needs.detect.outputs.architect == 'true' &&
      needs.quality.result != 'cancelled'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    env:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      VITE_FRESCO_PREVIEW_URL: ${{ secrets.VITE_FRESCO_PREVIEW_URL }}
      VITE_FRESCO_PREVIEW_API_TOKEN: ${{ secrets.VITE_FRESCO_PREVIEW_API_TOKEN }}
      # Preview bundles ship without analytics so PR-preview interaction
      # doesn't pollute the production PostHog project.
      VITE_DISABLE_ANALYTICS: 'true'
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - id: deploy
        name: Build and deploy architect-web preview
        run: |
          pnpm exec turbo run build --filter=@codaco/architect-web
          json=$(npx --yes netlify-cli@22 deploy --no-build \
                   --filter=@codaco/architect-web \
                   --dir=apps/architect-web/dist \
                   --site="${{ secrets.NETLIFY_SITE_ID_ARCHITECT }}" \
                   --alias="pr-${{ github.event.number }}" --json)
          echo "$json" | jq .
          url=$(echo "$json" | jq -r '.deploy_url')
          echo "url=$url" >> "$GITHUB_OUTPUT"

  # Single PR comment summarising whichever preview(s) deployed. Uses a hidden
  # HTML marker to locate the existing comment on subsequent runs so we update
  # in place rather than spamming the PR with one new comment per run.
  comment-preview-urls:
    needs: [deploy-docs-preview, deploy-architect-preview]
    if: |
      always() &&
      github.event_name == 'pull_request' &&
      (needs.deploy-docs-preview.result == 'success' || needs.deploy-architect-preview.result == 'success')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          DOCS_URL: ${{ needs.deploy-docs-preview.outputs.url }}
          ARCHITECT_URL: ${{ needs.deploy-architect-preview.outputs.url }}
        with:
          script: |
            const marker = '<!-- ci:deploy-previews -->';
            const lines = [marker, '🚀 **Deploy previews**'];
            const { DOCS_URL, ARCHITECT_URL } = process.env;
            if (DOCS_URL) lines.push(`- documentation → ${DOCS_URL}`);
            if (ARCHITECT_URL) lines.push(`- architect-web → ${ARCHITECT_URL}`);
            const body = lines.join('\n');
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100,
            });
            const existing = comments.find((c) => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  deploy-architect-prod:
    needs:
      [
        detect,
        quality,
        chromatic-fresco-ui,
        chromatic-interview,
        interview-e2e-report,
      ]
    if: |
      always() &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.detect.outputs.architect == 'true' &&
      needs.quality.result == 'success' &&
      needs.chromatic-fresco-ui.result != 'failure' &&
      needs.chromatic-interview.result != 'failure' &&
      needs.interview-e2e-report.result != 'failure'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    env:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      VITE_FRESCO_PREVIEW_URL: ${{ secrets.VITE_FRESCO_PREVIEW_URL }}
      VITE_FRESCO_PREVIEW_API_TOKEN: ${{ secrets.VITE_FRESCO_PREVIEW_API_TOKEN }}
      # Production bundle has analytics enabled (no VITE_DISABLE_ANALYTICS set).
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - id: deploy
        name: Build and deploy architect-web production
        run: |
          pnpm exec turbo run build --filter=@codaco/architect-web
          json=$(npx --yes netlify-cli@22 deploy --no-build --prod \
                   --filter=@codaco/architect-web \
                   --dir=apps/architect-web/dist \
                   --site="${{ secrets.NETLIFY_SITE_ID_ARCHITECT }}" --json)
          echo "$json" | jq .
          url=$(echo "$json" | jq -r '.deploy_url')
          echo "url=$url" >> "$GITHUB_OUTPUT"

  docs-preview-checks:
    # Run link-check + redirect-test against the preview URL only. Production
    # deploys aren't gated on these — the equivalent checks have already
    # passed against the preview before the change reached main.
    needs: [deploy-docs-preview]
    if: needs.deploy-docs-preview.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - name: Wait for Netlify CDN propagation
        # netlify-cli's deploy returns as soon as the deploy is queued, but
        # asset URLs are served from edges that may take a few seconds to
        # propagate. The link-checker fans out many parallel requests; without
        # this wait it hits FETCH_ERROR on assets that resolve fine moments
        # later. Cheap and reliable.
        env:
          DOCS_URL: ${{ needs.deploy-docs-preview.outputs.url }}
        run: |
          for i in 1 2 3 4 5 6; do
            if curl -fsSL --max-time 10 -o /dev/null "$DOCS_URL/en"; then
              echo "deploy is reachable; proceeding"
              break
            fi
            echo "attempt $i: not yet, sleeping 5s"
            sleep 5
          done
      - name: Dead-link check
        env:
          DOCS_URL: ${{ needs.deploy-docs-preview.outputs.url }}
        # @jthrilly/dead-link-checker fans out many parallel fetches against
        # the Netlify preview and gets transient FETCH_ERRORs even though the
        # URLs resolve fine on retry. Wrap in a short retry loop so we don't
        # fail the PR on flake.
        run: |
          attempt=1
          max=3
          until npx --yes @jthrilly/dead-link-checker "$DOCS_URL" -v --yes; do
            if (( attempt >= max )); then
              echo "dead-link-checker failed after $max attempts"
              exit 1
            fi
            echo "attempt $attempt failed, retrying in 10s"
            attempt=$((attempt + 1))
            sleep 10
          done
      - name: Redirect test
        working-directory: apps/documentation
        env:
          DEPLOYMENT_URL: ${{ needs.deploy-docs-preview.outputs.url }}
        run: pnpm run test-redirects

  chromatic-fresco-ui:
    needs: [quality, detect]
    if: needs.detect.outputs.fresco_ui_storybook == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - run: pnpm exec turbo run build-storybook --filter=@codaco/fresco-ui
      - working-directory: packages/fresco-ui
        env:
          CHROMATIC_PROJECT_TOKEN: ${{ secrets.CHROMATIC_PROJECT_TOKEN_FRESCO_UI }}
          # Forward PR context so the chromatic CLI doesn't fall back to the
          # merge commit (GH Actions checks out detached HEAD on pull_request).
          CHROMATIC_BRANCH: ${{ github.head_ref || github.ref_name }}
          CHROMATIC_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          CHROMATIC_SLUG: ${{ github.repository }}
        # --storybook-build-dir points chromatic at the storybook-static
        # directory turbo just produced (or restored from cache); without it
        # chromatic re-runs `pnpm build-storybook` into a tmp dir and the
        # turbo cache savings evaporate.
        run: npx --yes chromatic --exit-once-uploaded --auto-accept-changes main --storybook-build-dir=storybook-static

  chromatic-interview:
    needs: [quality, detect]
    if: needs.detect.outputs.interview_storybook == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - run: pnpm exec turbo run build-storybook --filter=@codaco/interview
      - working-directory: packages/interview
        env:
          CHROMATIC_PROJECT_TOKEN: ${{ secrets.CHROMATIC_PROJECT_TOKEN_INTERVIEW }}
          # Forward PR context — see chromatic-fresco-ui for rationale.
          CHROMATIC_BRANCH: ${{ github.head_ref || github.ref_name }}
          CHROMATIC_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          CHROMATIC_SLUG: ${{ github.repository }}
        # --storybook-build-dir points chromatic at the storybook-static
        # directory turbo just produced (or restored from cache); without it
        # chromatic re-runs `pnpm build-storybook` into a tmp dir and the
        # turbo cache savings evaporate.
        run: npx --yes chromatic --exit-once-uploaded --auto-accept-changes main --storybook-build-dir=storybook-static

  interview-e2e:
    needs: [quality, detect]
    if: needs.detect.outputs.interview_e2e == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      slug: ${{ steps.meta.outputs.slug }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - id: meta
        env:
          REF: ${{ github.head_ref || github.ref_name }}
        run: |
          SLUG=$(printf '%s' "$REF" | tr '/' '-' | tr '[:upper:]' '[:lower:]')
          # Strip any chars that would break GitHub Pages URLs or filesystem paths.
          SLUG=$(printf '%s' "$SLUG" | tr -c '[:alnum:]-_.' '-')
          SLUG=${SLUG:-unknown}
          printf 'slug=%s\n' "$SLUG" >> "$GITHUB_OUTPUT"
      - name: Run E2E tests
        run: ./packages/interview/e2e/scripts/run.sh
      - name: Reclaim artifact ownership
        if: always()
        run: |
          sudo chown -R "$(id -u):$(id -g)" \
            packages/interview/e2e/playwright-report \
            packages/interview/e2e/test-results 2>/dev/null || true
      - name: Upload report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: interview-playwright-report
          path: packages/interview/e2e/playwright-report/
          if-no-files-found: warn
          retention-days: 14

  interview-e2e-report:
    needs: interview-e2e
    if: ${{ !cancelled() && needs.interview-e2e.result != 'skipped' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
      pull-requests: write
    concurrency:
      group: interview-e2e-pages-deploy
      cancel-in-progress: false
    environment:
      name: github-pages
      url: https://complexdatacollective.github.io/network-canvas-monorepo/interview-e2e/${{ needs.interview-e2e.outputs.slug }}/
    steps:
      - name: Download new report
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: interview-playwright-report
          path: new-report
      - name: Fetch existing pages content
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: gh-pages
          path: existing-pages
        continue-on-error: true
      - name: Merge report into branch subdirectory
        env:
          SLUG: ${{ needs.interview-e2e.outputs.slug }}
        run: |
          mkdir -p merged
          if [ -d existing-pages ] && [ "$(ls -A existing-pages 2>/dev/null)" ]; then
            cp -r existing-pages/. merged/
            rm -rf merged/.git
          fi
          rm -rf "merged/interview-e2e/$SLUG"
          mkdir -p "merged/interview-e2e/$SLUG"
          cp -r new-report/. "merged/interview-e2e/$SLUG/"
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: merged/
      - uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          SLUG: ${{ needs.interview-e2e.outputs.slug }}
          OUTCOME: ${{ needs.interview-e2e.result }}
        with:
          script: |
            const { SLUG, OUTCOME } = process.env;
            const url = `https://complexdatacollective.github.io/network-canvas-monorepo/interview-e2e/${SLUG}/`;
            const emoji = OUTCOME === 'success' ? '✅' : '❌';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `${emoji} Interview E2E — **${OUTCOME}**\n\n📊 [View report](${url})`,
            });

  release:
    needs: quality
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    env:
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
    outputs:
      interviewer_v7_released: ${{ steps.detect_interviewer_v7.outputs.released }}
      interviewer_v7_version: ${{ steps.detect_interviewer_v7.outputs.version }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
          fetch-depth: 2
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - name: Build all publishable packages
        # Only build the workspaces under packages/ — those are what changesets
        # publishes to npm. Apps (./apps/*) are private:true and don't need to
        # build successfully here; their deploy pipeline is per-app deploy jobs.
        run: pnpm exec turbo run build --filter='./packages/*'
      - uses: changesets/action@63a615b9cd06ba9a3e6d13796c7fbcb080a60a0b # v1
        with:
          version: pnpm run version-packages
          publish: pnpm run publish-packages
          createGithubReleases: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
      - name: Detect interviewer-v7 version bump
        # changesets/action either opens a "Version Packages" PR or, when no
        # changesets remain, runs the publish step which has already mutated
        # apps/interviewer-v7/package.json in-place during the merged version
        # PR. Diff the file's version against HEAD^ to decide whether to
        # trigger an Electron build downstream. fetch-depth: 2 above makes
        # HEAD^ available.
        id: detect_interviewer_v7
        env:
          PKG_JSON: apps/interviewer-v7/package.json
        run: |
          current=$(node -p "require('./$PKG_JSON').version")
          if git cat-file -e "HEAD^:$PKG_JSON" 2>/dev/null; then
            previous=$(git show "HEAD^:$PKG_JSON" | node -p "JSON.parse(require('fs').readFileSync(0,'utf8')).version")
          else
            previous=""
          fi
          if [[ "$current" != "$previous" && -n "$current" ]]; then
            echo "released=true" >> "$GITHUB_OUTPUT"
            echo "version=$current" >> "$GITHUB_OUTPUT"
            echo "interviewer-v7 version changed: $previous -> $current"
          else
            echo "released=false" >> "$GITHUB_OUTPUT"
            echo "version=" >> "$GITHUB_OUTPUT"
            echo "interviewer-v7 version unchanged ($current)"
          fi

  # Per-platform Electron build for interviewer-v7. Triggered only when the
  # release job above detected a version bump for @codaco/interviewer-v7.
  # macOS leg signs + notarizes via Apple credentials in repo secrets;
  # Windows ships unsigned (SmartScreen warning is acceptable for now);
  # Linux builds AppImage + deb unsigned.
  interviewer-v7-build:
    needs: release
    if: needs.release.outputs.interviewer_v7_released == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
            artifact_name: interviewer-v7-macos
          - os: windows-latest
            platform: win
            artifact_name: interviewer-v7-windows
          - os: ubuntu-latest
            platform: linux
            artifact_name: interviewer-v7-linux
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      # No --ignore-scripts: electron-builder install-app-deps must rebuild
      # better-sqlite3-multiple-ciphers against the runner's Electron ABI.
      - run: pnpm install --frozen-lockfile
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cache/electron
            ~/.cache/electron-builder
          key: ${{ runner.os }}-electron-${{ hashFiles('apps/interviewer-v7/package.json') }}
          restore-keys: ${{ runner.os }}-electron-
      # Turbo cache is keyspaced by runner.os; only the ubuntu leg can hit
      # the cache written by the quality job. Mac/Windows rebuild deps from
      # scratch (~1-2 min) — acceptable for now.
      - if: matrix.os == 'ubuntu-latest'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-
      - name: Build workspace dependencies
        # ^... builds deps of interviewer-v7 (fresco-ui, interview, etc.)
        # but NOT interviewer-v7 itself — electron-vite handles that.
        run: pnpm exec turbo run build --filter=@codaco/interviewer-v7^...
      - name: Decode App Store Connect API key
        if: matrix.os == 'macos-latest'
        env:
          APPLE_API_KEY_B64: ${{ secrets.APPLE_API_KEY }}
        run: |
          key_path="${RUNNER_TEMP}/AuthKey.p8"
          printf '%s' "$APPLE_API_KEY_B64" | base64 --decode > "$key_path"
          echo "APPLE_API_KEY=${key_path}" >> "$GITHUB_ENV"
      - name: Build Electron app (${{ matrix.platform }})
        working-directory: apps/interviewer-v7
        env:
          # macOS-only credentials. Non-mac legs see empty strings, which
          # electron-builder treats as "not configured" (no signing, no
          # notarization). Linux has no signing concept; Windows ships
          # unsigned per the spec.
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          CSC_LINK: ${{ matrix.os == 'macos-latest' && secrets.CSC_LINK || '' }}
          CSC_KEY_PASSWORD: ${{ matrix.os == 'macos-latest' && secrets.CSC_KEY_PASSWORD || '' }}
        run: pnpm electron:dist:${{ matrix.platform }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ matrix.artifact_name }}
          path: apps/interviewer-v7/release-builds/*
          if-no-files-found: error
          retention-days: 30

  # Collect all platform artifacts and create one GitHub Release at tag
  # `interviewer-v7@v<version>`. make_latest:false keeps the repo's "Latest
  # release" pointer from bouncing between apps as different apps cut
  # versions out of this monorepo.
  interviewer-v7-publish:
    needs: [release, interviewer-v7-build]
    if: needs.release.outputs.interviewer_v7_released == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 10
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts
          pattern: interviewer-v7-*
          merge-multiple: true
      - name: List collected artifacts
        run: ls -la artifacts/
      - uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: interviewer-v7@v${{ needs.release.outputs.interviewer_v7_version }}
          name: Interviewer v7 v${{ needs.release.outputs.interviewer_v7_version }}
          files: artifacts/*
          fail_on_unmatched_files: true
          make_latest: 'false'
          generate_release_notes: true

  # Carry forward prior success statuses for conditional jobs that the
  # current push didn't touch. Push-to-push detection (see detect job)
  # deliberately re-runs a job ONLY when its package changed between
  # consecutive PR commits, but GitHub renders the latest workflow run's
  # check list — so jobs that ran successfully on an earlier PR commit
  # would appear "skipped" on the new tip and (depending on branch
  # protection config) block merge.
  #
  # This step republishes a check_run on the current SHA — same name,
  # conclusion "success" — pointing at the prior run that did succeed.
  # Branch protection sees the most-recent check_run for each name and
  # treats the job as passing without burning runner time re-executing
  # work that's still valid.
  #
  # First push on a PR: no prior runs exist, so nothing to carry; the
  # detect logic already runs the relevant jobs at least once because
  # the push diff IS the cumulative PR diff on the first commit.
  carry-forward-statuses:
    needs:
      - detect
      - deploy-docs-preview
      - deploy-architect-preview
      - chromatic-fresco-ui
      - chromatic-interview
      - interview-e2e
      - interview-e2e-report
      - docs-preview-checks
    if: always() && github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: write
    steps:
      - uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          # Map detect flag → list of conditional jobs gated on it.
          # interview-e2e-report transitively depends on interview-e2e, and
          # docs-preview-checks on deploy-docs-preview, so they share flags.
          FLAG_DOCS: ${{ needs.detect.outputs.docs }}
          FLAG_ARCHITECT: ${{ needs.detect.outputs.architect }}
          FLAG_FRESCO_UI_STORYBOOK: ${{ needs.detect.outputs.fresco_ui_storybook }}
          FLAG_INTERVIEW_STORYBOOK: ${{ needs.detect.outputs.interview_storybook }}
          FLAG_INTERVIEW_E2E: ${{ needs.detect.outputs.interview_e2e }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        with:
          script: |
            const flagToJobs = {
              FLAG_DOCS: ["deploy-docs-preview", "docs-preview-checks"],
              FLAG_ARCHITECT: ["deploy-architect-preview"],
              FLAG_FRESCO_UI_STORYBOOK: ["chromatic-fresco-ui"],
              FLAG_INTERVIEW_STORYBOOK: ["chromatic-interview"],
              FLAG_INTERVIEW_E2E: ["interview-e2e", "interview-e2e-report"],
            };

            // Only consider jobs the current run didn't execute (flag false).
            // If a flag is true, the real job ran on this commit and its own
            // check_run already reflects the outcome — no carry-forward needed.
            const skippedJobs = Object.entries(flagToJobs).flatMap(
              ([flag, jobs]) => (process.env[flag] === "false" ? jobs : []),
            );
            if (skippedJobs.length === 0) {
              core.info("No skipped conditional jobs on this run; nothing to carry forward.");
              return;
            }

            // Walk prior workflow runs on this PR's source branch, most recent
            // first, looking for the latest successful execution of each
            // skipped job. We stop walking once every skipped job is resolved.
            const remaining = new Set(skippedJobs);
            const resolutions = new Map();
            let page = 1;
            const PER_PAGE = 50;
            const MAX_RUNS = 200; // safety cap; PRs rarely have 200 runs.
            let scanned = 0;

            outer: while (remaining.size > 0 && scanned < MAX_RUNS) {
              const { data } = await github.rest.actions.listWorkflowRunsForRepo({
                owner: context.repo.owner,
                repo: context.repo.repo,
                branch: process.env.PR_HEAD_REF,
                event: "pull_request",
                per_page: PER_PAGE,
                page,
              });
              if (data.workflow_runs.length === 0) break;

              for (const run of data.workflow_runs) {
                scanned += 1;
                // Skip the run we're currently inside of.
                if (run.id === context.runId) continue;
                // Skip runs against the same commit (re-runs of the same SHA
                // don't introduce new information).
                if (run.head_sha === process.env.PR_HEAD_SHA) continue;
                // Pull the job list for this run.
                const { data: jobs } = await github.rest.actions.listJobsForWorkflowRun({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  run_id: run.id,
                  per_page: 100,
                });
                for (const job of jobs.jobs) {
                  if (!remaining.has(job.name)) continue;
                  if (job.conclusion === "success") {
                    resolutions.set(job.name, { run, job });
                    remaining.delete(job.name);
                  }
                }
                if (remaining.size === 0) break outer;
              }
              if (data.workflow_runs.length < PER_PAGE) break;
              page += 1;
            }

            for (const [name, { run, job }] of resolutions) {
              await github.rest.checks.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name,
                head_sha: context.sha,
                status: "completed",
                conclusion: "success",
                details_url: job.html_url,
                output: {
                  title: `Carried forward from ${run.head_sha.substring(0, 7)}`,
                  summary: [
                    `This job ran successfully on an earlier commit of this PR and the latest push did not touch its package, so it was not re-executed.`,
                    ``,
                    `Source run: [#${run.run_number}](${run.html_url}) on commit \`${run.head_sha.substring(0, 7)}\`.`,
                  ].join("\n"),
                },
              });
              core.info(`Carried forward ${name} from run #${run.run_number} (${run.head_sha.substring(0, 7)}).`);
            }

            for (const name of remaining) {
              core.info(`No prior successful run found for ${name}; leaving its skipped status as-is.`);
            }
