# Studio #1243 pause and resume handoff

Paused at the user's request on 2026-09-13 to preserve the remaining weekly usage budget. Do not restart work until the user resumes. This is the current entry point; the longer `2026-09-13-studio-1243-resumed-delivery.md` is historical evidence, not an ordered current task list.

## Last landed work

GitHub verified the last merge in this epic's implementation stack was PR **#1741**, `chore(studio): generate reviewed subprocessor inventory`, at **2026-09-13 09:02:46 UTC** (11:02:46 Africa/Johannesburg), merge `6ff61a19685d054d6b4089a06c0f7b212efd2459`. PR#1740 shared migration execution merged at08:50:02UTC, `3c09307b9e29f4cb07ae6f7f865b08b2a2211d19`. Subsequent repository merges were protocol-builder work from another effort. No PR from this stack merged in the resumed review session after those. Numerous committed branch corrections are not landed product work.

## Durable workspace map

Local base: `/Users/jmh629/.codex/worktrees/afe1/network-canvas-monorepo/.claude/worktrees/studio-1243`. Each name below is a directory beneath it. Feature and integration branches are pushed to GitHub; do not recreate from `/tmp`. Public specification is a separate repository: `complexdatacollective/network-canvas-specifications`, PR#1. Other PRs are in the monorepo. The coordinator's original root checkout is clean on `fix/studio-experiment-fixture-time` and must not be repurposed blindly.

| Worktree                        | Exact saved commit                         | Local branch                                         |
| ------------------------------- | ------------------------------------------ | ---------------------------------------------------- |
| registry-integration            | `1cc1dc06ed45f40c9b966b3f3fa54f559efbde04` | `codex/studio-1243-registry-integration`             |
| public-specification-repository | `fa96b192f192a52bc27a7360b179375fd049ba8f` | `feat/template-registry-v1`                          |
| recovery-integration            | `5ef2e8434cf93c2bfaf15973c3d311850b87c04c` | `codex/studio-1243-recovery-integration`             |
| observability-integration       | `ae98d4adea800d910ffabba20347a932828cfca1` | `codex/studio-1243-observability-integration`        |
| registry-studio-flow            | `43724e8e398ac05c50368b568493cc4d15df74dd` | `checkpoint/studio-1243-registry-studio-flow`        |
| installer-integration           | `ad0951da68ad7469f4eb338f711c867451e08615` | `codex/studio-1243-installer-integration`            |
| audit-export-runtime            | `617703826f54c0772c63237d3a606390244b1819` | `codex/studio-1243-audit-export-runtime`             |
| webhook-runtime                 | `15a5a25d304ef53a65ac20c9932514b867ce299a` | `codex/studio-1243-webhook-runtime`                  |
| message-runtime                 | `ee11c47ff3f06febd48a5a4ce8d7aaefb5f2f0c9` | `codex/studio-1243-message-runtime`                  |
| timing-ingestion                | `1cd7afe919fd55dce4d25137db6bdd8ebd723748` | `checkpoint/studio-1243-timing-ingestion`            |
| managed-collector-runtime       | `c0c888e6e4e0cf0a95c7d6f7049f58cbc0a4e90e` | `wip/studio-1243-managed-collector-vitest-selection` |
| managed-database-operator       | `b3589a54e85c65ad04c07226800ff1d083929e16` | `codex/studio-1243-managed-database-operator`        |
| managed-anchor-runtime          | `356ed15b78c20bab1a4459b763db8e9f456c8eee` | `codex/studio-1243-managed-anchor-runtime`           |

Actual PR branch identities can differ from local names. Re-read live PR metadata before pushing. Recovery #1746 uses `feat/studio-recovery-authorization`; Registry #1745 uses `feat/studio-template-registry-service`; observability #1869 uses `checkpoint/studio-1243-observability-integration`; flow #1871 uses `checkpoint/studio-1243-registry-studio-flow`; installer #1870 uses `checkpoint/studio-1243-installer-integration`; export #1874 uses `codex/studio-1243-audit-export-runtime`; webhook #1876 uses `checkpoint/studio-1243-webhook-runtime`; message #1877 and timing #1878 use their checkpoint branches; collector #1880 uses its checkpoint branch; DB #1881 and anchor #1883 use their codex branches. Both actual and integration aliases were pushed for the forward chain.

## What is saved and verified

- Registry1cc v4 corrections propagated through recovery → observability → flow → installer → export → webhook. The agent stopped before message. No numbered migration drift. Fresh isolated PG18 tests: installer196, export187, webhook205; types and Knip passed. Webhook required `recovered: 0` in the Registry intent dispatch result after merging the shared result type.
- Installer's earlier5133 head passed full Linux CI run34777675890, including server tests and kernel-observed browser telemetry opt-out. The newer integrated ad095 head has local focused proof; it does not inherit exact-head Linux or distribution-release qualification.
- Astra cleared timing1cd7, managed DBb358, export30475 cleanup, and anchorabf51 month authorization slices. Later export findings remain, and later integrated/fixed heads require review again. Final whole-epic integrated Astra review has never completed.
- Anchor356ed15 fixes two new reproduced defects: old-month forwarder debits after UTC rollover and URL-escaped repository build paths.37 tests pass, one optional DynamoDB integration case skipped; lint has no errors and Knip passes. Rebuilt actual ZIP SHA256 `1b1fff882f1e0d58323fe427acba69bb5130336802e03dc222064036d22fc7aa`. Source and build fixtures include trusted clock injection and real copied-path closure build. This final correction has not received Astra review.
- Collector runner correction status is recorded in the final checkpoint addendum below. Its pre-fix fb7 transport tests prove NATS3.4.0 `Subscription.closed` exists, refuting that review premise; the separate Node/Vitest discovery defect is real.

## Outstanding source defects: resume here

1. **Registry #1745 is NOT cleared.** Astra reproduced four defects at1cc: kind/license/curated catalogue filters bypass shared/local scan admission; pending hard-deletion jobs with an unrelated audit event are accepted by recovery but cannot be executed; supported dormant operator grants are incorrectly rejected by recovery; recovery evidence omits immutable audit history and accepts a restore missing takedown/restore events. Public readiness probes also lack in-flight coalescing and can starve serving pools (source-confirmed; write a concurrent regression). GitHub comments4000731540/1542/1545/1550 plus Astra's admission reproduction. No implementation for these began before pause.
2. **Export #1874 has seven unaddressed findings** at30475, inherited by617703: failure events persist outcome succeeded; cleaned events precede actual object deletion; uppercase UUIDs fail encrypted handle AAD; retained-sweep docs describe an obsolete effect-deadline/two-pass protocol; multipart buffering recopies each accumulated part for every CSV row; failed jobs have no bounded retention cleanup; sparse-filter preflight scans run under the per-team audit lock. Root only inspected source; no export edits started. Exact review comments4000674938/4940/4941/4942 and4000709548/9554/9557.
3. **Public specification #1** has three fresh findings atfa96: safe-text patterns omit explicit NUL exclusion, ORCID/DOI regex end anchors allow trailing newline discrepancies, and curated=true lacks required curation metadata. Triage against runtime before fixing; retain idempotent re-publication's yanked=true response for previously withdrawn entries (review requesting unconditional false is disproved).
4. Complete collector runner correction validation and final anchor review, then propagate all verified parents through managed branches and runtime stack. Message/Twilio fixes at ee11 are saved, but webhook parent integration is explicitly blocked pending the user's exact approval. Timing0016 must be regenerated from the final integrated source, preserving all published0001–0015 bytes.
5. Re-run exact-head checks, request current-head reviews and resolve addressed/refuted threads once permitted. Audit at20:06UTC found **no merge-ready PR**; stacked PRs often have no substantive CI (zero or skipped jobs), and historical approvals do not establish current-head readiness. Last full live audit is in local `evidence/live-delivery-audit-2000.{json,md}`; fetch fresh state, since pushes supersede it.

## Outstanding epic delivery and live qualification

The active delivery children are **#1250 self-host distribution**, **#1251 managed cloud infrastructure**, and **#1252 observability**. #1244–#1249 and #1561 were closed at the live audit. #1253 billing is explicitly deferred, not an implicit new task.

- Merge the reviewed prerequisite/feature stack through the merge queue, verify origin/main ancestry, and run the controlled signed GHCR/SBOM release. The release workflow requires main; local images are not published distribution evidence.
- Prove checkout-free fresh installation, signed-in owner bootstrap, predecessor-image upgrade, populated backup/key recovery, PostgreSQL profile/performance and restart behavior on the final artifact. Existing unit/integration/Linux privacy proof is not a substitute.
- Provision and qualify staging/production: provider database privileges/tuning, encrypted storage/key custody, PITR≤5min/RTO≤4h, independently administered backups and key restore drill, annual rotation, TLS/DNS, deployment rollback, measured cost≤$100/month.
- Qualify real operator/researcher alert delivery, tenant isolation, PostHog error path, queue metrics and 30-day managed retention. No live provider endpoint, paid resource or production qualification was created in this session.
- Finish a separate **Astra adversarial review of the final integrated implementation** before claiming completion. Sol remains the main orchestrator; use efficient models for implementation and retain one writer per worktree.

## Exact external blockers: do not bypass

Five automatic-review rejections require the user's pending specific answers despite standing authorization. Do not retry through another tool, branch or agent:

1. Integrating webhook e185 ancestry into message d519 (message nowee11). A later webhook merge still includes the blocked operation.
2. Resolving eight verified timing PR1878 discussions: thread suffixes h6c91,h6c93,h6c96,h6c97,h6ln8,h6ln-,h6ln_,h6wtP. Full IDs are in local `evidence/timing-current-thread-identities.json`.
3. Replying to/resolving message PR1877 comments4000501857/1863/1866.
4. Replying to/resolving six webhook PR1876 comments4000490663/0665/0669/1178/1181/1184 and requesting fresh review.
5. Updating installer PR1870's stale description. Exact replacement text is local `evidence/installer-pr-current-body.md`; it reports Linux success while retaining release gates. Its source hash will need refreshing after the next permitted update.

Automatic review said these external review/description mutations lacked specific authorization; the integration rejection also prohibits workaround execution. The earlier migration conflict approvals do not answer these later specific blocks. All pending questions were asked once; do not repeatedly ask them.

An additional refusal blocked extracting existing test passwords from Docker metadata. A materially safer new, isolated PG fixture with newly generated credentials completed the necessary tests, then was removed. Do not recover existing credentials from container metadata. No real secrets belong in Git or handoff output.

Provider access is also outstanding. Prior search found no Fly/Crunchy/AWS KMS/B2/Cloudflare credentials. Netlify access and the approved domains do not provide the persistent backend. User has already been asked which existing provider accounts to use; never infer account ownership or purchase permission beyond the stated hosting cap.

## Resuming safely

1. Read this note and the adjacent preserved Astra reproduction text files. Inspect clean/dirty state and real remote PR heads before editing. Read applicable repository skills once; no blanket permission questions for previously authorized work.
2. Use durable feature branches/worktrees and pushed checkpoints, commit as Joshua Melville <joshua@northwestern.edu>, without assistant attribution. Keep scopes reviewable and do not merge unverified changes to main.
3. Node24 path: `/Users/jmh629/.local/share/fnm/node-versions/v24.18.0/installation/bin`. Some old worktrees make pnpm attempt a no-TTY reinstall; use existing `node_modules/.bin` and direct Node test/tsc/Knip runners. Do not purge dependencies reflexively.
4. New real PG fixtures need `max_prepared_transactions=16` and appropriate Studio allowed-login configuration. The Registry fresh-process test needs an isolated database, not merely a schema, because admission inspects database-wide security metadata.
5. Take the five Registry findings and seven export findings as separate parallel implementation scopes. Finalize current fixes in a single reviewed push per scope. Rebuild and rerun only evidence invalidated by subsequent changes; preserve usage by avoiding repeated broad tests and unchanged status polling.

Local detailed evidence lives at BASE/evidence: `astra-registry-pause.md`, `astra-registry-reproduction/`, `registry-integration-pause.md`, `collector-pause.md`, `runtime-review-2011.json`, `registry-review-2026.json`, `live-delivery-audit-2000.{json,md}`, and the focused red/green logs. The important Astra reproductions are also preserved beside this tracked note to survive local temporary-file loss.

## Final collector checkpoint

Collector runner fix is preserved as **unverified WIP** commit `c0c888e6e4e0cf0a95c7d6f7049f58cbc0a4e90e` on separate branch `wip/studio-1243-managed-collector-vitest-selection`; PR1880 remains at fb7. The baseline Vitest `No test suite found` failure reproduced. WIP excludes the two node:test files from direct Vitest discovery and routes them serially through the existing wrapper with assertions that both suites actually emitted their expected result. Post-change verification was interrupted by dependency reinstall/network; hooks subsequently restored dependencies, but no passing post-change test exists. Run the focused wrapper plus types/lint/Knip before integrating this WIP. Do not report it fixed solely because it is committed.
