# Interviewer administration localization (#1617)

Status: implementation prepared; final shared form-error integration and integrated verification await explicit local integration approval. Branch: `feat/interviewer-app-i18n`, isolated worktree `/private/tmp/nc-i18n-interviewer`, originally main `4c4789f59`.

## Contract and ownership

The issue and its empty comments, both accepted localization designs (`2026-09-04-studio-ui-localization-design.md` and `2026-08-27-protocol-localization-design.md`), Studio adoption, and PR #1651 were read. App messages use explicit, described `interviewer.*` descriptors; genuinely universal actions use `common.*`. Production support is explicitly `en`, `en-GB`, and `es`. English is extracted, British English is a reviewed sparse override, and Spanish covers every app message.

This workstream owns `apps/interviewer/**`, this plan, its normal-lane Interviewer changeset, and the explicitly authorized three-line app dependency importer in `pnpm-lock.yaml`. The lead owns shared prerequisites and the separate PR/review sequence. Shared prerequisites have been merged normally, preserving ancestry, through `4cdd831f9d3677fb2a77fe01802377313d211e66`. No source or dependency symlinks point into another worktree. Commits use the user's configured identity.

## Surface inventory and acceptance evidence

| Surface                                   | Implemented behavior and evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root, boot, and device preference         | The provider mounts above auth, dialogs, toasts, and routes. Stored preference, browser best fit, then English use the shared resolver. Automatic mode removes the override; browser changes and cross-tab storage events update immediately. Tests cover malformed/unsupported preferences, blocked storage, reload, document `lang`/`dir`, autonyms, and development-only pseudo-localization. Boot loading uses the same preference before async startup.                                                                 |
| Language settings                         | Shared `LocaleSelect` provides en/en-GB/es autonyms and Automatic. Saving is independent of the vault and research records. A blocked storage write keeps the current choice and exposes localized guidance. Narrow settings reuse the shared horizontal Tabs layout; desktop settings retain the side layout. A browser test verifies usable control width and arrow-key tab navigation at 390px.                                                                                                                           |
| Home, deck, and navigation                | Welcome/empty/loading states, protocol deck cards and actions, sample/development teasers, import pending feedback, resume, view switching, and accessibility names are localized. Real import/start/resume/data/delete-cancel flows preserve authored study names, case identifiers, and stored research data.                                                                                                                                                                                                              |
| Protocol import and administration errors | File, validation, migration, launch-file failures, deletion, and retry guidance use app or shared message identities. Real malformed archives verify Spanish remediation. Optional technical validation details retain `lang=en dir=ltr` for copying to support. Destructive confirmations explain irreversible effects in the active language.                                                                                                                                                                              |
| Settings                                  | Every About, Language, Interview, Data export, Privacy, Security, and Synthetic data section is converted, including counts, percentages, sizes, labels, hints, buttons, and statuses. Real browser tests inspect all seven sections and controls in Spanish.                                                                                                                                                                                                                                                                |
| Data and exports                          | Table columns, filters, sort labels, row/bulk selection, progress, completion, deletion, and export dialog actions are localized. Stable IDs and export fields are unchanged. Export state stores the shared stage ID and formats `exportStageMessages` on render, so active progress follows a language switch. Dates/numbers and whole-sentence ICU plural/select messages respect the active locale.                                                                                                                      |
| Authentication and setup                  | Welcome, all wizard steps, nested skip/no-security confirmations, lock/recovery screens, all unlock methods, authenticator management, and step-up copy are converted. Operational auth results retain stable descriptors plus diagnostic English. Queued ReactNode copy uses `AppMessage`; submitted form errors use shared `createMessageError` and shared display-time error rendering. A real submitted PIN regression covers an error surviving a locale switch without resubmission, plus retry in the production PWA. |
| Synthetic repair guidance                 | Shared stable constraint reason metadata and translated validation-rule labels provide actionable Spanish repair instructions. Authored entity/variable names stay intact. Raw `conflict.reason` is not the primary guidance.                                                                                                                                                                                                                                                                                                |
| PWA/offline                               | All supported app and shared catalogs are statically bundled. Production build integrity checks verify the service worker, manifest, critical chunks, and 69 stage-preview assets. The browser test waits for a controlling worker, first switches to Spanish while offline, reloads, imports the bundled sample, starts an interview, and switches/reloads en-GB without a catalog request.                                                                                                                                 |
| Participant language boundary             | Schema-8 interviews remain English under a nested provider with document ownership disabled and a full-size `lang=en dir=ltr` boundary. The enclosing administration document retains its selected locale. Participant dialogs live inside that boundary. Real form, resume, completion, and sociogram interaction tests exercise the rendered boundary. Runtime protocol localization (#1313/schema 9) remains separately owned.                                                                                            |
| Catalog and literal audit                 | 445 English descriptors and complete Spanish translations, four en-GB overrides, extraction freshness, namespace/description checks, ICU argument/tag parity, and JSX literal lint. Product names, authored content, research data, file paths, IDs, export schemas, release-note content, and diagnostic/test fixtures are deliberate boundaries.                                                                                                                                                                           |

## Translation review

Spanish was AI translated and independently AI reviewed by the lead; no human linguistic review is claimed. The lead read all 444 IDs / 416 unique English–Spanish pairs in the frozen `/private/tmp/nc-review-interviewer.json` snapshot. All five findings were applied:

- `interviewer.sampleProtocol.description`: codebooks uses “diccionarios de datos”.
- `interviewer.settingsDialog.includeCSVFilesAttributesEdgesEgoIn`: network edges uses “vínculos”.
- `interviewer.dataView.allStrongLengthStrongOnThisPage`, `interviewer.dataView.allStrongSelectedCountStrongMatchingInterviewsAre`, and `interviewer.dataView.selectAllTotalCountMatching` use natural singular “Se ha seleccionado … entrevista” and “Seleccionar # coincidencia”, preserving the all/count meanings.
- `interviewer.dataViewColumns.stepProgress` formats the known total as a number while retaining the unknown-total marker.
- `interviewer.settingsDialog.currentTotalInterviewsGenerated` uses whole ICU singular/plural branches and formats current/total values.

Short ambiguous labels received 41 improved contextual translator descriptions. The biometric setup guidance says “continue”, matching its shared action label. The final Automatic option was shortened consistently to “Automatic (browser)” / “Automático (navegador)” to fit the narrow native selector.

The lead also independently reviewed and accepted the final eight changed English–Spanish pairs and 35 changed descriptions from the previously frozen unique-pair sample, with no further findings. The final app catalog contains 445 IDs after the generic error heading moved to the already-reviewed shared `common.genericError` descriptor, the file-read boundary gained its own retry guidance, and the native export picker gained a file-type descriptor.

The common-message audit compared every app message with all 12 shared descriptors. The generic failure heading now uses `common.genericError`, including its shared punctuation. The 22 common call sites in 13 files cover 12 cancel, three close, two delete, two continue, one loading, and two generic errors. `Save…` remains contextual because it opens the platform save/share step. There are no exact duplicate universal app messages. A separate toast API audit found zero `toast.promise` or destructured promise calls: all administration toasts use options with `AppMessage` in `title`/`description`, including the ref-held callbacks. No React element is passed as a promise-options object. Extraction and the updated error-boundary/catalog checks passed all nine tests (`/private/tmp/interviewer-common-heading-green.log`). A final import audit then reproduced a file-read rejection before the existing import catch: the new positive Spanish-guidance assertion failed on an empty toast region. The boundary now resolves with localized reselect-file guidance, starts no import/write callback, and permits reselection. All nine focused import/catalog checks pass (`/private/tmp/interviewer-unreadable-import-red.log`, `interviewer-unreadable-import-green.log`). The lead independently reviewed and accepted its one new Spanish entry in `/private/tmp/interviewer-spanish-unreadable-file-delta.json`.

The JSX literal rule additionally covers custom-component `label`, `placeholder`, `title`, and `itemLabel` props, preserving the plugin's default ARIA/image checks. The full source census passes with no untranslated custom-prop findings (`/private/tmp/interviewer-custom-props-census.log`). Temporarily replacing the synthetic interview-count field label with a literal produces exactly one error, from `formatjs/no-literal-string-in-jsx`; restoring the descriptor passes (`/private/tmp/interviewer-custom-props-mutation-red.log`, `interviewer-custom-props-mutation-restored.log`). No mutation remains.

The final AST census inspected 138 production source files and 115 string-valued property/callback candidates outside descriptor declarations. It found one additional administration literal: the native export picker's file type. `interviewer.sessionMutations.zipArchive` now owns “ZIP archive” / “Archivo ZIP”; the lead independently AI reviewed and accepted this pair (`/private/tmp/interviewer-spanish-native-picker-delta.json`). The sole production `saveBlob` caller synchronously formats the label from the active locale; all 15 direct helper test calls provide their description explicitly. A real hook/helper regression builds in English, cancels, switches to Spanish, and saves the same archive. Its positive native-picker option assertion failed before the correction (`/private/tmp/interviewer-native-picker-red.log`), then all 40 export-flow, save-ladder, and catalog tests passed (`/private/tmp/interviewer-native-picker-green.log`). The test also verifies native invocation before the first await, unchanged filename/MIME/blob, no marking on cancellation, and successful marking only after the save. Remaining census entries are authored or sample protocol content, platform/vendor names, stable identifiers, and labeled technical diagnostics. No mutation remains.

This workstream also independently reviewed shared Spanish catalogs, all 831 Fresco frozen entries plus its subsequent TOTP descriptor, ten failure-guidance entries, and two passkey fallback descriptors, Architect frozen pairs 565–1244, final Architect delta entries 120–449, and the final 32-entry property-census delta. A separate Fresco census inspected 270 production files, 182 property/callback candidates and 334 further literals; its one in-scope finding was an English passkey fallback stored at three registration sites. The owner corrected it and both new fallback translations passed independent AI review. The final Architect pairs passed semantic review; adjacent queued row-label composition exposed a frozen translated noun and bare-noun grammar, reported to the owner for correction. Findings were sent to those owners; this is separate from this app's independent review. Two bounded Architect test-only passes verified 135 queued-dialog tests and 103 descriptor/factory/label tests. The second pass exposed an encoded-error rendering bug and tightened five primary-guidance assertions that fail on that bug; the owner fixed the production alert. A third pass adapted 13 error-contract test files while preserving refused-save and unchanged-data assertions: 128/130 passed, with two complete repair-sentence assertions waiting for the shared input-controls wording. These contributions are not counted as Interviewer acceptance evidence.

An independent bounded review of prerequisite PR #1702 at `9f84828869fcb3f3e85ff68fb27181a046b98f02` reproduced a shared field race: a language switch during whole-form async revalidation of an existing error can leave its final guidance in the previous language. The real Form/Field reproduction preserves values and refuses submission; the positive Spanish assertion fails (`/private/tmp/nc-shared-locale-race-review.log`). The lead owns the repair and regression. The review also identified an invalid extensionless no-parser target in the Next setup documentation. The packed core runtime imports for protocol validation, synthetic generation, and network export passed with a resolver explicitly forbidding optional app-i18n imports (`/private/tmp/nc-shared-optional-core-review.log`).

## Oracles, regressions, and baseline decisions

Intentional mutations establish positive failures, rather than relying only on negative assertions:

| Guard                       | Deliberate mutation and observed failure                                                                                                                                                                                                                                                         |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Immediate preference update | Removing `setCurrentPreference(next)` prevented the Spanish combobox from appearing; restoring it passed all 13 provider cases. `/private/tmp/interviewer-provider-red.log`.                                                                                                                     |
| Complete Spanish catalog    | Blank Spanish language label caused the catalog guard to name the missing translation. `/private/tmp/interviewer-catalog-red.log`.                                                                                                                                                               |
| Live export progress        | Formatting the stable stage with English caused the Spanish announcement assertion to fail; restoration passed. `/private/tmp/interviewer-export-stage-mutation-red.log` and `interviewer-export-stage-mutation-restored.log`.                                                                   |
| Queued wizard copy          | Replacing the first queued title with frozen English caused the cross-tab Spanish assertion to fail; restoration passed. `/private/tmp/interviewer-queued-wizard-mutation-red.log` and `interviewer-queued-wizard-mutation-restored.log`.                                                        |
| Narrow settings             | The real side rail left approximately 110px for controls and failed the horizontal layout assertion. Reusing top Tabs produces a native control wider than 240px and passes keyboard navigation. `/private/tmp/interviewer-narrow-settings-red.log` and `interviewer-narrow-settings-green.log`. |
| Submitted PIN errors        | The app regression initially exposed the missing shared display-time transport. Final integrated mutation and production checks remain to be recorded.                                                                                                                                           |

Live tests caught two genuine participant sizing regressions introduced by the new boundary. Its complete flex/size constraints restored participant forms and sociograms. `InterviewComplete` explicitly takes the available width, restoring the original centered completion layout. Neither regression was adopted as an expected screenshot.

Canonical Linux generation passed 5/5 twice (`/private/tmp/interviewer-visual-canonical-first.log`, `interviewer-visual-canonical-corrected.log`). Exactly two app PNGs are intentionally changed: `settings-about.png` and `settings-data-export.png`. The new Language row moves later navigation rows, and the footer uses the correct singular “1 protocol”. The prior forced decimal place for storage percentages was restored. Before/after images and each changed row band were inspected. The corrected completion differed only by small antialiasing values; its original committed PNG was restored. The lead independently inspected and accepted `/private/tmp/settings-about-reviewed-comparison.png` and `/private/tmp/settings-data-export-reviewed-comparison.png`: navigation changes are intentional and content/controls remain intact. A final canonical control on the final shared tree remains required.

Spanish desktop/tablet/phone captures cover all seven settings sections, home, and security setup. The first phone review revealed the cramped side rail; the top-tab correction restores full-width content. The lead independently inspected the Spanish language/synthetic phone captures and accepted horizontal tabs and wrapping. The old Automatic label was visibly clipped; the source now says “Automatic (browser)” / “Automático (navegador)”. Final captures will be refreshed after the final production build and handed to the lead for inspection; keyboard and scrolling reachability remain explicit assertions. The extended phone workflow passes End-key selection of the last tab and native Tab traversal to the enabled Generate action, verifying that the long panel scrolls it into the viewport (`/private/tmp/interviewer-narrow-keyboard-scroll-green.log`). It waits for the import completion toast before querying protocol-dependent controls, since the deck previews the imported name before the database write. This is a built-app checkpoint result; it will run again after final integration.

## Verification record

Use the repository runtime for every command:

```sh
export PATH=/Users/jmh629/.local/share/fnm/node-versions/v24.18.0/installation/bin:$PATH
```

The host default Node 26 caused unrelated jsdom storage failures; Node 24.18.0 resolves them. The final app manifest/importer agrees, and isolated frozen installation passed. No temporary dependency removals remain.

The last complete checkpoint before submitted-error transport and the narrow settings additions passed:

| Command                                             | Result / log                                                                                                                                                                        |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm --filter @codaco/interviewer typecheck`       | Passed, `/private/tmp/interviewer-typecheck-reviewed.log`                                                                                                                           |
| `pnpm --filter @codaco/interviewer test`            | 623/623 in 87 files, `/private/tmp/interviewer-unit-reviewed.log`                                                                                                                   |
| `pnpm --filter @codaco/interviewer test:storybook`  | 97/97 in 36 files, `/private/tmp/interviewer-storybook-final.log`                                                                                                                   |
| `pnpm --filter @codaco/interviewer build`           | Production and PWA integrity passed; 89 precache entries, 69 stage assets, all critical chunks. `/private/tmp/interviewer-build-responsive.log` includes the responsive correction. |
| `pnpm --filter @codaco/interviewer test:e2e:native` | 44/44, `/private/tmp/interviewer-native-final.log`                                                                                                                                  |
| `pnpm exec knip --workspace apps/interviewer`       | Full scoped check passed, `/private/tmp/interviewer-knip-final.log`                                                                                                                 |
| Scoped oxlint with type-aware rules                 | Zero errors; existing warnings recorded in `/private/tmp/interviewer-lint-reviewed.log`                                                                                             |
| App extraction and oxfmt                            | Passed after final app copy changes; final combined checks remain below.                                                                                                            |

The latest draft-preparation checks passed extraction and all 40 focused export-hook/save-ladder/catalog tests, full scoped Knip (`/private/tmp/interviewer-knip-precommit.log`), E2E TypeScript (`/private/tmp/interviewer-e2e-typecheck-precommit.log`), and full app source lint with zero errors (`/private/tmp/interviewer-lint-precommit.log`). The native-picker touched-file lint has zero warnings or errors (`/private/tmp/interviewer-native-picker-lint.log`). Current app typecheck (`/private/tmp/interviewer-typecheck-precommit.log`) reports exactly six missing-export errors for the shared `createMessageError` / `formatMessageError` APIs in five app files, all supplied by the pending prerequisite. No other type errors are reported. These are draft checkpoint results; they do not replace the required final integrated type/build/unit/Storybook/PWA/native/visual gates.

## Current external approval blocker

Automatic approval review rejected both the ordinary local merge and the narrower `git merge --ff-only feat/app-i18n-spanish-prerequisites` update to verified checkpoint `fd9b7de4fda7a6dedccd6567e9e63fbf5524b40e`. Its stated reason was that the user's “Do not merge” restriction also requires confirmation for a local fast-forward. No history mutation occurred. The lead has requested explicit authorization and this operation remains pending; shared patches or indirect ref changes will not bypass the rejection.

Unaffected final checks passed: all 20 provider/catalog/export guard tests in three files (`/private/tmp/interviewer-independent-final-guards.log`), formatting of all 318 matched files (`/private/tmp/interviewer-format-final.log`), full scoped Knip, zero lint errors, and changeset isolation. The final 445-ID Spanish snapshot is `/private/tmp/nc-review-interviewer-final.json`; its eight changed pairs and reviewed-description delta are in `/private/tmp/nc-review-interviewer-delta.json`.

## Next executable action

After explicit authorization, merge the lead's final shared prerequisite normally, preserving ancestry; the app snapshot commit means this will be an ordinary local merge rather than a fast-forward. Run the final app typecheck, unit and Storybook suites, lint/Knip, extraction/format/changeset guards, production build, submitted-PIN mutation, and all native E2E. Refresh and inspect Spanish captures, run canonical baseline control without accepting unexplained changes, record exact results here, then commit as the user. Coordinate the separate #1617 PR with the prerequisite and follow the user's latest-head Codex review loop. Do not claim completion of PR/review delivery before that evidence exists.
