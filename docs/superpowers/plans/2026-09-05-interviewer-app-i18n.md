# Interviewer application and built-in interview localization (#1617)

Status: ready PR [#1703](https://github.com/complexdatacollective/network-canvas-monorepo/pull/1703) implements Interviewer administration and the user's September 6 clarification: built-in interview controls plus home/setup/menu language selection. Shared prerequisite [#1702](https://github.com/complexdatacollective/network-canvas-monorepo/pull/1702) is merged. Runtime follow-up [#1719](https://github.com/complexdatacollective/network-canvas-monorepo/pull/1719), checkpoint `5128f4942c67dee5e8690fd43852887b23a24593`, is integrated normally at `b53b0486e2fbb9a53efa701448384aa23a37c83f`; the app PR will remain stacked on that branch until the lead retargets it to main. The earlier local-integration and push holds are removed. Final local verification and the installed/offline proof are recorded below; canonical CI images and a new exact-head Codex review remain required. The clean review of pushed `d91e308b713fbcb22f11b93d76782c834796e1ce` is historical, not a verdict on the integrated changes. The Case ID thread remains open until the lead inspects and the verified fix is pushed. Branch: `feat/interviewer-app-i18n`, isolated worktree `/private/tmp/nc-i18n-interviewer`, originally main `4c4789f59`.

## Contract and ownership

The issue and its empty comments, both accepted localization designs (`2026-09-04-studio-ui-localization-design.md` and `2026-08-27-protocol-localization-design.md`), Studio adoption, and PR #1651 were read. App messages use explicit, described `interviewer.*` descriptors; genuinely universal actions use `common.*`. Production support is explicitly `en`, `en-GB`, and `es`. English is extracted, British English is a reviewed sparse override, and Spanish covers every app message.

This workstream owns `apps/interviewer/**`, this plan, its normal-lane Interviewer changeset, and the explicitly authorized three-line app dependency importer in `pnpm-lock.yaml`. The lead owns shared prerequisites and the separate PR/review sequence. Shared prerequisites and the runtime follow-up have been merged normally, preserving ancestry, through `5128f4942c67dee5e8690fd43852887b23a24593`. The user authorized PR merges; the lead owns GitHub merge sequencing and post-merge verification. No source or dependency symlinks point into another worktree. Commits use the user's configured identity.

## Surface inventory and acceptance evidence

| Surface                                   | Implemented behavior and evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root, boot, and device preference         | The provider mounts above auth, dialogs, toasts, and routes. Stored preference, browser best fit, then English use the shared resolver. Automatic mode removes the override; browser changes and cross-tab storage events update immediately. Tests cover malformed/unsupported preferences, blocked storage, reload, document `lang`/`dir`, autonyms, and development-only pseudo-localization. Boot loading uses the same preference before async startup.                                                                                                                                                  |
| Language settings                         | Shared `LocaleSelect` provides en/en-GB/es autonyms and Automatic. Saving is independent of the vault and research records. A blocked storage write keeps the current choice and exposes localized guidance. Narrow settings reuse the shared horizontal Tabs layout; desktop settings retain the side layout. A browser test verifies usable control width and arrow-key tab navigation at 390px.                                                                                                                                                                                                            |
| Home, deck, and navigation                | Welcome/empty/loading states, protocol deck cards and actions, sample/development teasers, import pending feedback, resume, view switching, and accessibility names are localized. Real import/start/resume/data/delete-cancel flows preserve authored study names, case identifiers, and stored research data.                                                                                                                                                                                                                                                                                               |
| Protocol import and administration errors | File, validation, migration, launch-file failures, deletion, and retry guidance use app or shared message identities. Real malformed archives verify Spanish remediation. Optional technical validation details retain `lang=en dir=ltr` for copying to support. Destructive confirmations explain irreversible effects in the active language.                                                                                                                                                                                                                                                               |
| Settings                                  | Every About, Language, Interview, Data export, Privacy, Security, and Synthetic data section is converted, including counts, percentages, sizes, labels, hints, buttons, and statuses. Real browser tests inspect all seven sections and controls in Spanish.                                                                                                                                                                                                                                                                                                                                                 |
| Data and exports                          | Table columns, filters, sort labels, row/bulk selection, progress, completion, deletion, and export dialog actions are localized. Stable IDs and export fields are unchanged. Export state stores the shared stage ID and formats `exportStageMessages` on render, so active progress follows a language switch. Dates/numbers and whole-sentence ICU plural/select messages respect the active locale.                                                                                                                                                                                                       |
| Authentication and setup                  | Welcome, all wizard steps, nested skip/no-security confirmations, lock/recovery screens, all unlock methods, authenticator management, and step-up copy are converted. Operational auth results retain stable descriptors plus diagnostic English. Queued ReactNode copy uses `AppMessage`; submitted form errors use shared `createMessageError` and shared display-time error rendering. The actual submitted PIN unit regression covers an error surviving a locale switch without resubmission and successful retry; the final built-app refusal/retry and production mutation both verify this behavior. |
| Synthetic repair guidance                 | Shared stable constraint reason metadata and translated validation-rule labels provide actionable Spanish repair instructions. Authored entity/variable names stay intact. Raw `conflict.reason` is not the primary guidance.                                                                                                                                                                                                                                                                                                                                                                                 |
| PWA/offline                               | All supported app and shared catalogs are statically bundled. Production build integrity checks verify the service worker, manifest, critical chunks, and 69 stage-preview assets. The browser test waits for a controlling worker, first switches to Spanish while offline, reloads, imports the bundled sample, starts an interview, and switches/reloads en-GB without a catalog request.                                                                                                                                                                                                                  |
| Built-in interview language               | The clarified contract removes the blanket English boundary. The host passes its resolved locale and persistent preference callback to Shell; Shell negotiates against its own en/en-GB/es registry, translates built-in controls, and owns the language of its existing main region. The host retains document ownership. Protocol-authored strings and research data remain literal; protocol-content localization (#1313/schema 9) remains separately owned. The actual three-browser menu/form/queued-dialog checks and the installed offline launch verify the integrated runtime.                       |
| Catalog and literal audit                 | 446 English descriptors and complete Spanish translations, four en-GB overrides, extraction freshness, namespace/description checks, ICU argument/tag parity, and JSX literal lint. Product names, authored content, research data, file paths, IDs, export schemas, release-note content, and diagnostic/test fixtures are deliberate boundaries.                                                                                                                                                                                                                                                            |

## Translation review

Spanish was AI translated and independently AI reviewed by the lead; no human linguistic review is claimed. The lead read all 444 IDs / 416 unique English–Spanish pairs in the frozen `/private/tmp/nc-review-interviewer.json` snapshot. All five findings were applied:

- `interviewer.sampleProtocol.description`: codebooks uses “diccionarios de datos”.
- `interviewer.settingsDialog.includeCSVFilesAttributesEdgesEgoIn`: network edges uses “vínculos”.
- `interviewer.dataView.allStrongLengthStrongOnThisPage`, `interviewer.dataView.allStrongSelectedCountStrongMatchingInterviewsAre`, and `interviewer.dataView.selectAllTotalCountMatching` use natural singular “Se ha seleccionado … entrevista” and “Seleccionar # coincidencia”, preserving the all/count meanings.
- `interviewer.dataViewColumns.stepProgress` formats the known total as a number while retaining the unknown-total marker.
- `interviewer.settingsDialog.currentTotalInterviewsGenerated` uses whole ICU singular/plural branches and formats current/total values.

Short ambiguous labels received 41 improved contextual translator descriptions. The biometric setup guidance says “continue”, matching its shared action label. The final Automatic option was shortened consistently to “Automatic (browser)” / “Automático (navegador)” to fit the narrow native selector.

The lead also independently reviewed and accepted the final eight changed English–Spanish pairs and 35 changed descriptions from the previously frozen unique-pair sample, with no further findings. The prior administration checkpoint contained 445 IDs after the generic error heading moved to the already-reviewed shared `common.genericError` descriptor, the file-read boundary gained its own retry guidance, and the native export picker gained a file-type descriptor.

The common-message audit compared every app message with all 12 shared descriptors. The generic failure heading now uses `common.genericError`, including its shared punctuation. The 22 common call sites in 13 files cover 12 cancel, three close, two delete, two continue, one loading, and two generic errors. `Save…` remains contextual because it opens the platform save/share step. There are no exact duplicate universal app messages. A separate toast API audit found zero `toast.promise` or destructured promise calls: all administration toasts use options with `AppMessage` in `title`/`description`, including the ref-held callbacks. No React element is passed as a promise-options object. Extraction and the updated error-boundary/catalog checks passed all nine tests (`/private/tmp/interviewer-common-heading-green.log`). A final import audit then reproduced a file-read rejection before the existing import catch: the new positive Spanish-guidance assertion failed on an empty toast region. The boundary now resolves with localized reselect-file guidance, starts no import/write callback, and permits reselection. All nine focused import/catalog checks pass (`/private/tmp/interviewer-unreadable-import-red.log`, `interviewer-unreadable-import-green.log`). The lead independently reviewed and accepted its one new Spanish entry in `/private/tmp/interviewer-spanish-unreadable-file-delta.json`.

The JSX literal rule additionally covers custom-component `label`, `placeholder`, `title`, and `itemLabel` props, preserving the plugin's default ARIA/image checks. The full source census passes with no untranslated custom-prop findings (`/private/tmp/interviewer-custom-props-census.log`). Temporarily replacing the synthetic interview-count field label with a literal produces exactly one error, from `formatjs/no-literal-string-in-jsx`; restoring the descriptor passes (`/private/tmp/interviewer-custom-props-mutation-red.log`, `interviewer-custom-props-mutation-restored.log`). No mutation remains.

The final AST census inspected 138 production source files and 115 string-valued property/callback candidates outside descriptor declarations. It found one additional administration literal: the native export picker's file type. `interviewer.sessionMutations.zipArchive` now owns “ZIP archive” / “Archivo ZIP”; the lead independently AI reviewed and accepted this pair (`/private/tmp/interviewer-spanish-native-picker-delta.json`). The sole production `saveBlob` caller synchronously formats the label from the active locale; all 15 direct helper test calls provide their description explicitly. A real hook/helper regression builds in English, cancels, switches to Spanish, and saves the same archive. Its positive native-picker option assertion failed before the correction (`/private/tmp/interviewer-native-picker-red.log`), then all 40 export-flow, save-ladder, and catalog tests passed (`/private/tmp/interviewer-native-picker-green.log`). The test also verifies native invocation before the first await, unchanged filename/MIME/blob, no marking on cancellation, and successful marking only after the save. Remaining census entries are authored or sample protocol content, platform/vendor names, stable identifiers, and labeled technical diagnostics. No mutation remains.

This workstream also independently reviewed shared Spanish catalogs, all 831 Fresco frozen entries plus its subsequent TOTP descriptor, ten failure-guidance entries, and two passkey fallback descriptors, Architect frozen pairs 565–1244, final Architect delta entries 120–449, and the final 32-entry property-census delta. A separate Fresco census inspected 270 production files, 182 property/callback candidates and 334 further literals; its one in-scope finding was an English passkey fallback stored at three registration sites. The owner corrected it and both new fallback translations passed independent AI review. The final Architect pairs passed semantic review; adjacent queued row-label composition exposed a frozen translated noun and bare-noun grammar, reported to the owner for correction. Findings were sent to those owners; this is separate from this app's independent review. Two bounded Architect test-only passes verified 135 queued-dialog tests and 103 descriptor/factory/label tests. The second pass exposed an encoded-error rendering bug and tightened five primary-guidance assertions that fail on that bug; the owner fixed the production alert. A third pass adapted 13 error-contract test files while preserving refused-save and unchanged-data assertions: 128/130 passed, with two complete repair-sentence assertions waiting for the shared input-controls wording. These contributions are not counted as Interviewer acceptance evidence.

An independent bounded review of prerequisite PR #1702 at `9f84828869fcb3f3e85ff68fb27181a046b98f02` reproduced a shared field race: a language switch during whole-form async revalidation of an existing error can leave its final guidance in the previous language. The real Form/Field reproduction preserves values and refuses submission; the positive Spanish assertion fails (`/private/tmp/nc-shared-locale-race-review.log`). The lead owns the repair and regression. The review also identified an invalid extensionless no-parser target in the Next setup documentation. The packed core runtime imports for protocol validation, synthetic generation, and network export passed with a resolver explicitly forbidding optional app-i18n imports (`/private/tmp/nc-shared-optional-core-review.log`).

## Oracles, regressions, and baseline decisions

Intentional mutations establish positive failures, rather than relying only on negative assertions:

| Guard                       | Deliberate mutation and observed failure                                                                                                                                                                                                                                                         |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Immediate preference update | Removing `setCurrentPreference(next)` prevented the Spanish combobox from appearing; restoring it passed all 13 provider cases. `/private/tmp/interviewer-provider-red.log`.                                                                                                                     |
| Complete Spanish catalog    | Blank Spanish language label caused the catalog guard to name the missing translation. `/private/tmp/interviewer-catalog-red.log`.                                                                                                                                                               |
| Live export progress        | Formatting the stable stage with English caused the Spanish announcement assertion to fail; restoration passed. `/private/tmp/interviewer-export-stage-mutation-red.log` and `interviewer-export-stage-mutation-restored.log`.                                                                   |
| Queued wizard copy          | Replacing the first queued title with frozen English caused the cross-tab Spanish assertion to fail; restoration passed. `/private/tmp/interviewer-queued-wizard-mutation-red.log` and `interviewer-queued-wizard-mutation-restored.log`.                                                        |
| Narrow settings             | The real side rail left approximately 110px for controls and failed the horizontal layout assertion. Reusing top Tabs produces a native control wider than 240px and passes keyboard navigation. `/private/tmp/interviewer-narrow-settings-red.log` and `interviewer-narrow-settings-green.log`. |
| Submitted PIN errors        | The app regression initially exposed the missing shared display-time transport. Final integrated mutation and production checks remain to be recorded.                                                                                                                                           |

At the earlier administration checkpoint, live tests caught two genuine participant sizing regressions introduced by the then-English boundary. Its complete flex/size constraints restored participant forms and sociograms. `InterviewComplete` explicitly takes the available width, restoring the original centered completion layout. Neither regression was adopted as an expected screenshot.

Canonical Linux generation passed 5/5 twice (`/private/tmp/interviewer-visual-canonical-first.log`, `interviewer-visual-canonical-corrected.log`). At that checkpoint, exactly two app PNGs were intentionally changed: `settings-about.png` and `settings-data-export.png`. The new Language row moves later navigation rows, and the footer uses the correct singular “1 protocol”. The prior forced decimal place for storage percentages was restored. Before/after images and each changed row band were inspected. The corrected completion differed only by small antialiasing values; its original committed PNG was restored. The lead independently inspected and accepted `/private/tmp/settings-about-reviewed-comparison.png` and `/private/tmp/settings-data-export-reviewed-comparison.png`: navigation changes are intentional and content/controls remain intact. A final canonical control on the final shared tree remains required.

Spanish desktop/tablet/phone captures cover all seven settings sections, home, and security setup. The first phone review revealed the cramped side rail; the top-tab correction restores full-width content. The lead independently inspected the Spanish language/synthetic phone captures and accepted horizontal tabs and wrapping. The old Automatic label was visibly clipped; the source now says “Automatic (browser)” / “Automático (navegador)”. Final captures will be refreshed after the final production build and handed to the lead for inspection; keyboard and scrolling reachability remain explicit assertions. The extended phone workflow passes End-key selection of the last tab and native Tab traversal to the enabled Generate action, verifying that the long panel scrolls it into the viewport (`/private/tmp/interviewer-narrow-keyboard-scroll-green.log`). It waits for the import completion toast before querying protocol-dependent controls, since the deck previews the imported name before the database write. This is a built-app checkpoint result; it will run again after final integration.

## Verification record

Use the repository runtime for every command:

```sh
export PATH=/Users/jmh629/.local/share/fnm/node-versions/v24.18.0/installation/bin:$PATH
```

The host default Node 26 caused unrelated jsdom storage failures; Node 24.18.0 resolves them. The final app manifest/importer agrees, and isolated frozen installation passed. No temporary dependency removals remain.

The last complete checkpoint before submitted-error transport and the narrow settings additions passed:

| Command                                             | Result / log                                                                                                                                                                        |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm --filter @codaco/interviewer typecheck`       | Passed, `/private/tmp/interviewer-typecheck-reviewed.log`                                                                                                                           |
| `pnpm --filter @codaco/interviewer test`            | 623/623 in 87 files, `/private/tmp/interviewer-unit-reviewed.log`                                                                                                                   |
| `pnpm --filter @codaco/interviewer test:storybook`  | 97/97 in 36 files, `/private/tmp/interviewer-storybook-final.log`                                                                                                                   |
| `pnpm --filter @codaco/interviewer build`           | Production and PWA integrity passed; 89 precache entries, 69 stage assets, all critical chunks. `/private/tmp/interviewer-build-responsive.log` includes the responsive correction. |
| `pnpm --filter @codaco/interviewer test:e2e:native` | 44/44, `/private/tmp/interviewer-native-final.log`                                                                                                                                  |
| `pnpm exec knip --workspace apps/interviewer`       | Full scoped check passed, `/private/tmp/interviewer-knip-final.log`                                                                                                                 |
| Scoped oxlint with type-aware rules                 | Zero errors; existing warnings recorded in `/private/tmp/interviewer-lint-reviewed.log`                                                                                             |
| App extraction and oxfmt                            | Passed after final app copy changes; final combined checks remain below.                                                                                                            |

The latest draft-preparation checks passed extraction and all 40 focused export-hook/save-ladder/catalog tests, full scoped Knip (`/private/tmp/interviewer-knip-precommit.log`), E2E TypeScript (`/private/tmp/interviewer-e2e-typecheck-precommit.log`), and full app source lint with zero errors (`/private/tmp/interviewer-lint-precommit.log`). The native-picker touched-file lint has zero warnings or errors (`/private/tmp/interviewer-native-picker-lint.log`). Current app typecheck (`/private/tmp/interviewer-typecheck-precommit.log`) reports exactly six missing-export errors for the shared `createMessageError` / `formatMessageError` APIs in five app files, all supplied by the pending prerequisite. No other type errors are reported. These are draft checkpoint results; they do not replace the required final integrated type/build/unit/Storybook/PWA/native/visual gates.

## First PR review and corrective verification

The Codex review requested in comment `5554131602` completed on the exact draft head at 2026-09-05 19:28:32 UTC. It reported two P2 findings, in threads `PRRT_kwDOKqiw4s6fmQjk` and `PRRT_kwDOKqiw4s6fmQjn`. This is a completed review with findings, not a clean verdict; the threads remain open pending the corrective push and integrated verification.

The locale provider had moved outside the recovery boundary. An actual `App` provider-ordering test deliberately throws during locale construction and positively requires visible English recovery and Reload. Before the correction, no dialog rendered; the separate Spanish later-provider recovery case already passed (`/private/tmp/interviewer-startup-boundary-red.log`). The existing provider-optional boundary now wraps locale construction, while the existing inner boundary retains localized handling. The fallback uses its own shared `ThemedRegion` with active `lang` and `dir`, so its real dialog portal cannot inherit Spanish left by bootstrap when English recovery is required. It does not take over or restore global document language. All five focused startup/boundary units pass (`/private/tmp/interviewer-startup-boundary-green.log`).

Three real Chromium boundary stories verify non-dismissible recovery, provider-optional English inside a Spanish surrounding region, and an open localized recovery dialog following English → Spanish → English storage events while the provider owns document language. Removing the fallback region's language attribute makes the positive portal assertion fail with actual `lang=es` instead of `en` (`/private/tmp/interviewer-fallback-region-mutation-red.log`); the attribute was restored. The new actual-provider story initially exposed a cold Vite reload when `@formatjs/intl-localematcher` was discovered mid-suite (`/private/tmp/interviewer-startup-boundary-storybook.log`). The lead released ownership of only its additive Interviewer `optimizeDeps.include` entry. After adding the full owner-chain entry and deleting this app's Storybook optimizer cache, all three stories pass without dependency reloads (`/private/tmp/interviewer-startup-boundary-storybook-cold.log`). The pending shared `react-intl/server` config change was not copied into this branch.

The Case ID required-string change also reproduced on the current app's older shared base: its actual form loses the visible error after English → Spanish (`/private/tmp/interviewer-case-id-switch-red.log`). The regression requires the exact Spanish Case ID guidance, blank value and invalid state preservation, refused empty submission, and successful submission of a corrected accented case identifier. An independent shared-only Form/Field probe on prerequisite `f3b4dc7b88441d0349d33af52203f6e30bc76c17` passes the same translated-required-string contract; the original async whole-form locale-race probe also still passes (2/2, `/private/tmp/interviewer-case-id-shared-probe.log`). The shared stable registration correction therefore addresses the reviewed mechanism while retaining the app-specific Case ID text. No Interviewer source, catalog or dependency was copied into the prerequisite for this probe. The actual app regression was pending integration at that review checkpoint. The authorized integration and subsequent actual-app proof are recorded below; the lead still owns review-thread resolution.

The correction introduces no translation changes. Extraction, scoped Knip, formatting and diff checks pass (`/private/tmp/interviewer-review-fixes-extraction.log`, `interviewer-review-fixes-knip.log`, `interviewer-review-fixes-format.log`); current types still report only the six known missing shared error-transport exports (`/private/tmp/interviewer-review-fixes-typecheck.log`). Focused lint has no errors and one pre-existing default-import warning (`/private/tmp/interviewer-review-fixes-lint.log`). The lead inspected and accepted the complete correction and regression evidence before its coordinated app-only commit and normal push.

## Active-locale sorting census and correction

The follow-up read-only formatter census covered administration `toFixed`, `toLocale*`, `Intl` constructors, `localeCompare`, and list/error/filename joins. It found three real display-ordering omissions: protocol cards (`buildDeck`), DataView protocol-filter options, and session-query Case ID/protocol-name sorting. On an English device with Spanish selected, these retained English ordering, putting `Ñandú` before `Nube`. The actual database query also returned the wrong first record before pagination. Raw authored names remain literal; their administration ordering now follows the selected app locale.

`buildDeck` now accepts an optional locale with a provider-optional English default; its sole production caller, `ProtocolDeck`, passes `intl.locale` and includes it in the deck memo. All existing direct helper tests intentionally use the English default. `SessionQueryParams` likewise carries an optional locale with an English query default. The sole production query caller is `useSessionQuery` through the unchanged `lib/db/api.ts` forwarding facade. It obtains the active formatter and includes its locale in the query memo, triggering the existing guarded requery when language changes. `queryMatchingSessionIds` receives the same parameter object but intentionally ignores display order because it returns filter membership. The selection filter key also excludes locale so checked interview identities survive reordering. DataView's protocol options use an active-locale collator and memo dependency. No locale is written to research records or shared URLs.

Existing case-insensitive deck sorting, default lexical ordering of numeric-looking identifiers, numeric progress sorting, chronological dates, stable ID ties, raw names and hash/slot identities are preserved. The independent probe in `/private/tmp/interviewer-formatter-census/red.log` failed the deck and both paginated name-sort assertions with one English/Spanish control passing. New actual-app regressions then failed five positive assertions before the correction: open carousel locale switching, filter-option switching with the same protocol array, actual table/query switching, and both database name columns before pagination (`/private/tmp/interviewer-sorting-unit-red.log`). After the correction, all 35 cases in the three new suites and existing deck/encrypted-session suites pass (`/private/tmp/interviewer-sorting-unit-green.log`). The table test checks English → Spanish → English, retained checked-row identity and unchanged stored records; the carousel test preserves the active protocol and starts its original hash after reordering. The database cases check both directions, both pages, totals, all three production locales, provider-optional English and numeric/date/tie behavior. The tests retain real query/IndexedDB and table/carousel paths; only unrelated toolbar popup mechanics, per-card interview-start forms, and mutation/auth providers are replaced by visible test adapters.

The remaining five `localeCompare` calls are ISO timestamp ordering or stable record-ID ties. All six `.join` expressions in four modules are explicit original technical support reports/schema paths or React keys; displayed synthetic variable/rule lists already use active `intl.formatList`. No early user-facing filename/list joins or native fixed-decimal/locale-default number formatting remain. All four production `formatBytes` callers pass the active formatter. English `messageResult` formatting is the documented diagnostic Error.message transport, while visible message identities are retained until rendering. This correction adds no messages or translation delta.

Scoped lint and full repository Knip pass (`/private/tmp/interviewer-sorting-lint.log`, `/private/tmp/interviewer-sorting-knip.log`). Current app types still report exactly the six known missing shared error-transport exports and no new errors (`/private/tmp/interviewer-sorting-typecheck.log`). Extraction produces no catalog changes and all ten touched files pass formatting (`/private/tmp/interviewer-sorting-extraction.log`, `/private/tmp/interviewer-sorting-format.log`). The final 35-case control passes after formatting. The lead independently inspected all six production diffs, all three new regression files, the actual red/green logs and complete caller/exception census, and accepted the correction for the coordinated user-attributed commit and normal push. Live production/PWA/E2E checks still require shared integration.

## Ready-event review and CI checkpoint

The explicit second Codex verdict [5554422678](https://github.com/complexdatacollective/network-canvas-monorepo/pull/1703#issuecomment-5554422678) names `d81a2a202f` and reports no new findings. The lead independently inspected all four English/Spanish desktop/phone recovery captures and their bounds, visible focus and language assertions. On marking the PR ready, the summary records Code Review completed at 20:36:44 UTC and Security Review completed at 20:38:38 UTC on the same head. No additional finding threads or separate new verdict text were posted; the existing Case ID prerequisite thread remains open.

The user-requested feature dispatch [33990401105](https://github.com/complexdatacollective/network-canvas-monorepo/actions/runs/33990401105) used `force_run=true, release_app=none` on exact `d81a2a202f7b56b0587a9941ca6a1d632d319e6e`. Lint passed. Quality support failed the actual Interviewer PWA build and typecheck on the held `createMessageError`/`formatMessageError` exports. Units report 623 passed and five failed in 89 Interviewer files: PIN submitted-error rendering, reset-error rendering in two entry points, encoded auth-message transport, and Case ID locale retention. Each exercises the held shared error/field changes. Shared Interview Storybook discovers `react-intl/server` during the suite, reloads, and fails 60 setup files without running tests; the prerequisite already contains the verified optimizer correction. Logs are `/private/tmp/interviewer-ready-ci-quality-support.log`, `/private/tmp/interviewer-ready-ci-unit.log`, and `/private/tmp/interviewer-ready-ci-storybook.log`.

This feature dispatch deliberately selects zero E2E suites under the repository policy; all six pixel/native jobs were skipped. It does not replace the required local pinned native/PWA/canonical evidence after integration. At that checkpoint, the dev server on port 5184 could not load the app module because of the held missing export. The subsequently authorized local integration removes that failure; the same user-facing dev server remains running.

## Authorized integration and clarified runtime scope

The earlier automatic-review refusal of the local merge is resolved: the user explicitly authorized the operation, and the lead merged shared `f3b4dc7b88441d0349d33af52203f6e30bc76c17` normally into this feature branch. Both the app-owned `intl-localematcher` and shared `react-intl/server` optimizer entries remain present. No shared code was copied to bypass the earlier hold. The new merge uses Joshua Melville's configured author and committer identity. At that earlier checkpoint GitHub PR merging was not authorized. The user subsequently authorized PR merges, which remain lead-owned; force pushes, releases, and production deployment are not part of this task.

On September 6 the user clarified that Interviewer's preference must also localize built-in `@codaco/interview` controls, with a selector in the interview menu, on the home surface, and throughout setup. Protocol-authored text and collected data remain unchanged. This supersedes the earlier blanket English runtime boundary, and does not implement protocol-content localization.

The host reuses its existing `LanguageSettings` and shared `LocaleSelect`. A compact instance is available before setup begins and in all six wizard steps; the home footer opens a keyboard-operated shared popover with the same full selector. The six setup wrapper component types are created once when the wizard is queued, so language changes preserve PIN inputs, acknowledgments, submission refusal, and retry state. Automatic mode still removes the device override and follows browser negotiation; local preference writes do not modify the vault or research records.

The Interview route passes its resolved host locale as Shell's `requestedLocale`, its raw device preference as controlled `localePreference`, and its existing `setPreference` callback as `onLocaleChange`. Shell owns its own catalog and locale negotiation. The host does not merge its catalog into the runtime. The controlled raw value keeps an explicitly saved Spanish preference visibly selected when an interview is reopened, so Automatic can clear it directly; browser-negotiated Spanish with no override remains Automatic. An independent consumer review found that a runtime-local selector reset on the host acknowledgment, and that a fresh mount could not distinguish those two states. The lead owns the shared controlled-selection seam and real Shell regression; the app covers initial saved preference, direct reset after reload, and stable data/form state. The updated route suite passes 22/22 (`/private/tmp/interviewer-host-controlled-green.log`). An independent root-provider probe using the actual shared native selector passes both host acknowledgment and saved-preference/direct-reset cases; ignoring controlled preference via one test-time transform fails the positive selected-Spanish assertion with `__automatic` (`/private/tmp/interviewer-host-controlled-shared-probe-green.log`, `interviewer-host-controlled-shared-probe-red.log`). No root source was changed by that probe. Its researcher-reopen finish guidance is a subscribed React node under a document-independent host-catalog boundary, preserving the original host semantics even when a queued dialog renders under Shell's separate provider. The obsolete `ParticipantLanguageBoundary` has been removed. All production and test callers of that boundary were converted; `InterviewComplete` retains its existing full-width layout without an extra wrapper.

The hint `interviewer.language.hint` now explains built-in controls and the unchanged protocol/data boundary. The lead independently AI reviewed and accepted the changed English–Spanish pair. The new `interviewer.interview.finishConfirmationDescription` is also independently AI reviewed; its Spanish uses “sin finalizar” to match the actual data-table action. The app now has 446 English/Spanish entries and four sparse British English overrides. No human translation review is claimed.

This workstream independently AI reviewed all 64 root-owned English–Spanish navigation/runtime pairs and both British English overrides. Six concrete findings were accepted by the lead: `interview.runtime.offlineMap` must state the connectivity prerequisite without promising automatic loading; `canvas`, `nodeList`, `firstNode`, and `secondNode` use participant-friendly Placement area / Item list / First item / second item wording; `pair` uses active `intl.formatList` so names such as Ana and Irene receive the Spanish conjunction correctly. The remaining 58 pairs and both overrides passed meaning, grammar, ICU/tag/token, and recovery/exit-guidance review. The lead owns those source corrections and their tests. The final six corrections were independently reviewed and accepted: “No tienes conexión. El mapa no se cargará hasta que vuelvas a conectarte.” preserves the source condition; “Área de colocación”, “Lista de elementos”, “Primer elemento” and “segundo elemento” preserve the participant task without research jargon. The obsolete pair descriptor was removed in favor of locale-aware list formatting, leaving 63 root-owned active entries.

### Integrated checkpoint and focused host proof

The first actual combined build, before the new runtime scope, passed app types and production/PWA integrity (`/private/tmp/interviewer-integrated-typecheck.log`, `interviewer-integrated-build.log`; 89 precache entries and 69 stage assets). Full units initially reported 634 passed and one test-environment failure: the genuine invalid-submit focus path called `Element.scrollTo`, which jsdom does not supply. A narrowly scoped test setup stub restores that missing platform method; the real Case ID test now positively requires field focus after refusal. The actual Case ID and submitted-PIN cases pass 8/8 (`/private/tmp/interviewer-integrated-auth-unit-green.log`).

A test-only external transform replaces exactly one production Case ID required descriptor with frozen English. The actual app regression fails its positive “El ID del caso es obligatorio” assertion (`/private/tmp/interviewer-integrated-case-id-mutation-red.log`); the restored eight-case control passes. The test also verifies the blank value and invalid state, refused submission, retained focus, and successful retry using an accented Case ID. No source mutation remains. This proves the original review finding on the combined app; the lead will inspect the evidence before resolving its thread. A separate exactly-once transform replaces the PIN descriptor transport with frozen English and fails the positive “PIN incorrecto.” assertion (`/private/tmp/interviewer-integrated-pin-mutation-red.log`). The final production mutation and PWA retry repetition below complete this earlier pending gate.

A cold Storybook run passes all 99 tests in 36 files after removing only this app's optimizer cache (`/private/tmp/interviewer-integrated-storybook-cold-green.log`), and full repository lint/format and Knip pass (`interviewer-integrated-lint.log`, `interviewer-integrated-knip.log`). These are pre-clarification combined checkpoints; they do not replace final gates for the new runtime and chooser changes.

The new home/setup surface tests first failed all three positive selector assertions (`/private/tmp/interviewer-language-surfaces-red.log`). After the implementation, the four focused surface/provider/wizard/route suites pass 43/43 (`/private/tmp/interviewer-language-surfaces-green.log`). They exercise the actual shared selector, popover, wizard and PIN form: English–Spanish selection and remount; keyboard opening and Escape focus return; a refused PIN enrollment with all 16 entered digits and acknowledgment retained across a language change; localized existing error without resubmission; and successful retry into the next step. The host-route test deliberately retains the original finish-description node under a separately English provider and verifies that it follows host preference changes while interview payload identity, read count, auth state, and stored data remain intact.

Fresh isolated Chromium contexts against the running dev server provide preliminary 390×844 captures of Spanish welcome, first setup step, PIN setup and home popover. Selector widths are 268px, 274px and 260px, with visible focus and bounds inside the viewport; the popover returns keyboard focus to its trigger. Automatic mode follows an en-GB browser and survives reload with no stored override. Evidence: `/private/tmp/interviewer-language-surface-captures.json`, `interviewer-language-surface-capture.log`, and `interviewer-welcome-spanish-language-phone.png`, `interviewer-wizard-spanish-language-phone.png`, `interviewer-wizard-spanish-pin-language-phone.png`, and `interviewer-home-spanish-language-phone.png`. Captures wait for fonts and finite animations. These are dev checks, not final production/offline acceptance.

The initial PIN capture exposed a real shared layout defect: an eight-segment fieldset keeps the browser's `min-width: min-content`, occupying 440px within 326px of available dialog content. The last segment extends to x=472 on the 390px viewport. The simultaneous-bounds oracle fails on digit seven at x=416 before focus can horizontally scroll the clipped row (`/private/tmp/interviewer-pin-phone-bounds-red.log`). The lead supplied the shared fieldset `min-w-0` correction. Continuing the real workflow then exposed an app-owned centered unlock column with intrinsic 440px width: `UnlockLayout` now constrains its shared PIN/passphrase/biometric/recovery content column to available width. All 24 setup, confirmation and unlock segments now fit x=32–358 with 33.75×48px controls on the 390px viewport.

The complete retry also reproduced a focus defect in both the actual unit and Chromium: a fresh PIN field mounts while the submitting form is disabled, so native autofocus is ignored. The lead supplied a once-per-mount pending autofocus correction to the shared segmented field, consumed only when it becomes enabled. Both temporary shared patches were byte-compared against the lead's source and committed runtime checkpoint, then removed before the ordinary integration merge consumed them. They are now inherited from `3ffccd752d119f9b63103d07f5284a6ddc8d605a` and are not app-owned changes. The actual refined PIN/Case ID control passes 8/8 (`/private/tmp/interviewer-integrated-pin-focus-green.log`), including visible translated errors, cleared failed digits, restored first-digit focus, and a successful keyboard-only retry.

The real dev phone workflow now completes PIN enrollment after preserving all 16 entered digits and acknowledgment through Spanish–British English–Spanish changes; reload locks the actual vault, wrong PIN is refused, the existing error follows Spanish–British English–Spanish without changing the vault, and correct retry succeeds. The 24 input bounds and outcome record are `/private/tmp/interviewer-pin-phone-workflow.json`, with `/private/tmp/interviewer-pin-phone-bounds-green.log`. Refreshed setup and error images are `/private/tmp/interviewer-wizard-spanish-pin-language-phone.png` and `/private/tmp/interviewer-lock-spanish-pin-error-phone.png`; both were viewed, with readable labels, required marks, visible focus and complete rows. The two written app Playwright cases also pass against the existing dev server with one isolated worker (`/private/tmp/interviewer-pin-dev-e2e.log`, 2/2). Natural Tab traversal reaches the checked acknowledgment below the initial viewport and then Continue, with positive focus and in-viewport assertions. `/private/tmp/interviewer-dev-e2e-spanish-phone-pin-acknowledgment-keyboard.png` was viewed: the warning scrolls and the complete acknowledgment and actions remain readable. The two cases still require repetition against the final production build; their test title does not turn this temporary dev-server run into production evidence.

The new host callback and queued-description oracles also fail when an exactly-once transform drops preference persistence or freezes the retained host node in English (`/private/tmp/interviewer-integrated-callback-mutation-red.log`, `interviewer-integrated-queued-mutation-red.log`). The restored six-suite route/provider/home/setup/Case ID/StatusRow control passes 69/69 (`/private/tmp/interviewer-language-auth-bounded-green.log`), and source/E2E type-aware lint reports zero errors. The new home selector required two precise existing keyboard-order updates; all explanation, blur and activation assertions remain intact. A mis-forwarded pnpm test delimiter earlier ran all 639 units instead of the intended subset and surfaced exactly those two expected test adaptations plus the real PIN-focus defect (636 passed). It is retained as diagnostic evidence, not a passing final suite.

## Final integrated local verification

The final source inherits runtime `5128f4942c67dee5e8690fd43852887b23a24593` normally. The last shared merge changes only the already-reviewed Navigation story/audit and unrelated Studio SMTP source/lock entries beyond the production runtime used by the app build; it does not alter Interviewer, Fresco UI, or runtime production bytes. Both local merge commits use Joshua Melville as author and committer. The source freeze contains 23 app/changeset files, including the new language menu and surface tests; `/private/tmp/interviewer-final-verification/final-app-source.patch` has SHA-256 `67d5461ee31894ca2f029733d8d5afac9cbec3805f03f91a4513ed356aa1ac39`. Its per-file hashes are in `final-app-source-files.json`. No temporary shared source patch remains, and all three expected optimizer entries remain present.

All commands use Node 24.18.0 and pnpm 11.20.0 from this isolated worktree, with `--config.verify-deps-before-run=false`. A frozen full-workspace install supplies dependencies for repository-wide checks without changing the lockfile. Evidence is under `/private/tmp/interviewer-final-verification/`:

- `focused.log`: 77/77 across eight actual app suites. `units.log`: 640/640 across 93 files. `storybook-cold.log`: 99/99 across 36 files after removing only the app's Storybook optimizer cache; no mid-suite optimizer reload.
- `typecheck-final.log`, `extract.log`, `changesets.log`, `lint-full.log`, and `knip-full.log`: app/E2E types, extraction, changeset lane, full repository lint/format, and full Knip pass. The integrated type check first caught the new home button's unsupported `ghost` variant; it now uses the existing `text` variant and passes. Earlier full-root checks with only filtered dependencies failed on other workspace configurations; the full frozen install resolved that environment setup.
- `build.log`: the actual production build and PWA integrity check pass with 89 precache entries, 69 stage-preview assets and three critical chunks. The served preview's index is byte-identical to this build (SHA-256 `4d02c3d3f6760ed3324cb6ec2b7e604408a977b2b832c9b0ebc25cc8e1917e6f`). No app production code changed after the build.
- `unit-mutations.log` and its four individual logs: exactly-once test transforms freeze Case ID, submitted PIN or the retained finish description in English, or drop the host locale callback. Every mutation fails its positive Spanish/current-locale assertion. `mutation-restored.log` passes all 30 tests in the three actual restored suites. The Case ID test retains blank value, invalid state, focus, refused submission, accented retry value and successful save assertions.
- `production-mutations-red.log` and `pin-mutant.json`: a separately compiled Vite/FormatJS output changes exactly one PIN error transport and one queued finish subscription. Both actual browser tests pass their initial English checks, then fail at the whole Spanish error/description assertion. The normal production output was never replaced. The standard native run below is the restored production control.
- `native-locales.log` / `native-locales.json`: all ten language scenarios pass in Chromium and Firefox; nine pass in WebKit. They exercise stored and Automatic preferences, real menu changes without losing dirty values, queued finish cancel without writes, every Spanish settings section, real PIN enrollment/refusal/retry, full required guidance, and keyboard/scroll reachability at 390×844. The only extra WebKit failure is its offline reload described below. `native-final.log` is the final standard production control: all 49/49 pass, including both Chromium/WebKit viewport cases and the restored PIN/queued-description scenarios. `knip-post-merge.log` also passes on the final merged source. `lint-post-merge.log` completes full Oxlint with no errors, then reports only this newly updated plan needs formatting; `format-post-merge.log` is the restored full-repository formatting control after formatting the plan. No passing source lint was repeated just for a Markdown formatting change. The initial standard native run passed 48/49; its remaining test used the obsolete exact `No security` text. The corrected role-option assertion requires the entire `No security (not recommended) Skip app security. Your data will not be protected by the app.` warning; no production behavior was changed to accommodate it.

### Installed offline proof and browser limitation

`installed-pwa-headed.log` passes the complete installed-window workflow (1/1). The scratch test installs the actual build into a new task-only persistent Chromium profile through CDP, verifies OS `.netcanvas` file-handler registration, selects the real installed-window user preference, and launches the app through `PWA.launch`. A normal warming tab is positively not standalone; the actual app window is positively standalone before and after reload. The test enrolls a real PIN, first selects Spanish while offline, reloads and unlocks, imports the bundled protocol, starts an interview with an accented Case ID, completes required step-up PIN authentication, switches en/en-GB/Automatic in the real interview menu, and reloads/unlocks again. The complete stored protocol/session records and encrypted vault remain unchanged through locale changes; no catalog request occurs.

The installed Chromium target resets its emulated network status during navigation. To prevent that harness behavior from creating a false offline success, the final test also refuses actual HTTP connections from its isolated port-4187 server for the entire offline sequence. Independent uncached network probes fail before and after; the final server record shows availability false and five refused connections. Target network state is reapplied after navigation, and the final document reports both standalone and offline. `installed-pwa-evidence.json`, `installed-pwa.spec.ts`, `installed-pwa-server.mjs`, and `installed-standalone-spanish-interview.png` record the result. Cleanup uninstalls only that test app and removes its temporary profile; no temporary install profiles remain. The user's port-5184 process and user data are untouched.

WebKit's extra offline scenario fails with `page.reload: WebKit encountered an internal error`. An app-free minimal service worker that returns an inline navigation response reproduces exactly the same error after `context.setOffline(true)`; its Chromium and Firefox controls pass (2/3, `minimal-offline.log` and the three `minimal-offline*` scratch sources). This is a reproduced Playwright WebKit engine limitation, not a passing three-browser offline matrix or an established Interviewer defect. Chromium and Firefox app offline checks and the actual installed Chromium server-refusal proof pass. No app code or committed test was skipped to hide the limitation.

### Responsive and copy inspection

Nine refreshed production phone captures were directly inspected by this workstream and independently by the lead. `welcome-spanish-language-phone.png`, `wizard-spanish-language-phone.png`, `wizard-spanish-pin-language-phone.png`, `home-spanish-language-phone.png`, and `settings-spanish-automatic-phone.png` show complete Automatic wording and readable controls. `spanish-phone-complete-pin-setup.png`, `spanish-phone-pin-acknowledgment-keyboard.png`, `spanish-submitted-pin-error.png`, and `spanish-phone-keyboard-scrolled-action.png` show all eight segments in bounds, visible focus, full recovery/unlock actions, and keyboard reachability of the acknowledgment and synthetic action below the initial viewport. The wizard deliberately scrolls long warning content; the test reaches and checks the complete acknowledgment before proceeding. Bounds and focus assertions are in `responsive-captures.json` and `native-locales.json`. The selector widths are 258–274px and the PIN row occupies x=32–358 on a 390px viewport.

A final mechanical copy audit inspected all 13 English/Spanish entries matching language/locale/preview/participant/English/protocol-content and the corresponding source patterns. No remaining user-facing text claims that built-in interview controls stay English or ignore the app preference. The only language hint explicitly includes built-in controls and retains the authored-protocol/data exception. The two changed host pairs remain independently AI reviewed; no additional translation delta was introduced.

## Next executable action

The lead independently inspected all 23 file states, the source-patch hash, production/test/E2E changes and the installed-PWA evidence, and approved the user-attributed checkpoint and normal push once final static gates pass. Stack PR #1703 on runtime PR #1719, then produce and inspect canonical CI images before requesting the final exact-head review.

The current shipping skill requires all canonical PNG generation through the dedicated GitHub Actions workflow; do not generate baselines locally, either natively or in Docker. The earlier Docker results above are historical administration-checkpoint evidence. The push hold is lifted; after lead inspection, coordinate exact-source CI regeneration on the normally pushed app head. Inspect every changed artifact image against its predecessor and adopt only explained changes; home/setup controls and built-in runtime labels can affect more captures than the earlier two settings PNGs. Record the canonical workflow and stability result separately from local native/PWA evidence.

The lead resolves the Case ID thread only after inspecting the actual combined-app proof. Update the ready PR around the final behavior and request a fresh explicit full-SHA Codex review after canonical adoption. Feature `force_run=true, release_app=none` dispatches validate packages but select zero E2E under the currently inspected policy. After runtime #1719 lands, the lead retargets the app PR to main and requires automatic current-head CI/native gates before merging. Continue corrections until a completed clean current-head verdict, no unresolved threads, and all relevant checks are established. The user has authorized PR merges; the lead owns merge sequencing and post-merge ancestry verification. Keep the user's port-5184 server and user data intact.
