# Studio platform foundation delivery checkpoint

Epic #1243 remains incomplete. This document records delivery evidence and durable work ownership after recovery of the local worktrees on 2026-09-08.

## Durable workflow

Long-lived implementation work lives in named Git worktrees under the persistent repository workspace, never in `/tmp`. Each coherent increment receives a commit attributed to Joshua Melville and a remote checkpoint. Checkpoint branches preserve unfinished work; they are not evidence of release readiness. Raw test logs stay in the persistent evidence directory. Reviewed, sanitized results belong in this plan and the relevant PR. No secrets or live credentials belong in either Git or logs.

The primary agent verifies actual agent status and uses the three available subagent slots for independent bounded tasks. Dependencies and privileged test commands are unblocked centrally when an implementation agent cannot run them. Separate worktrees and PostgreSQL ports prevent concurrent edits or fixture collisions. Each worktree has a single committer: the lead uses a dedicated coordination worktree for ledger updates while agents own implementation trees. Never run concurrent Git index or commit-hook operations in one worktree. No worktree is removed until its commits are reachable from a remote branch and all uncommitted work has been preserved.

## Current delivery

| Work                               | Evidence                                                                                                                                                                                                                                                                                                                            | Remaining gate                                                                                                                                                                                                    |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Managed estate/cost model PR #1734 | Merged as `4869147db0357807fa6565e4bbd2b978aa788d2c`; reviewed head verified as an ancestor of main                                                                                                                                                                                                                                 | Live provider qualification remains separate                                                                                                                                                                      |
| Fly preparation PR #1735           | Merged as `9ba638abc90a3f3729ee686db5e6591e061870d0`; reviewed head `f958397a84d7f6822b2f444c4b5ca854c3e07075` verified in origin/main; 142 scoped tests and all required CI passed                                                                                                                                                 | Live deployment remains separate                                                                                                                                                                                  |
| Managed ingress PR #1736           | Merged as `45a1ba2dad9699c68af8d943f6d3618980e83be4`; reviewed head `cddf19b22dff22d3ba044e71b6bf4a190b782fcf` verified in origin/main; all required CI passed. Live GET/HEAD200 without Location disproved duplicate redirect findings                                                                                             | Actual qualified cutover remains pending                                                                                                                                                                          |
| PII PR #1718                       | Final head `3f6cf628e` includes CI fixes `8a5933c32`, managed-ingress integration `e5c8cec87` and explicit bootstrap fixtures. 179 PostgreSQL regressions, 6 ingress tests, types/lint/Knip passed; repeated-initialization and late-ingress-gate mutants failed as intended. Independent review cleared the production integration | Full integrated run had 1915 passes and two bootstrap-fixture failures; all 11 bootstrap tests pass after correction. Current-head CI and review are pending. Do not infer full-suite success from a wrapper step |
| KMS PR #1723                       | Current head `4720f1e5c` integrates final PII/main. 67 focused custody/process/backup/key-initialization/ingress tests, types/lint/Knip passed. Earlier exact `3786c04c2` image passed two complete capture/restore tests                                                                                                           | Exact current image `studio-kms-restore:integrated-4720f1e5c` is building for repeat qualification. Current-head review pending; merge PII parent and retarget to main for required CI                            |
| Alert preferences                  | Alternate implementation preserved through `a307b8798`; types/lint/Knip, 14 alert tests, 6 migration tests, 68 route tests and recipient guard mutant passed                                                                                                                                                                        | Canonical PII already implements these settings in TeamSettings.tsx and audit/alerts.ts; do not merge duplicate APIs/schema                                                                                       |
| Recovery authorization             | Signed selective reauthorization commit `912e640f91a115b5864ad7e0e5e15086795260a9` pushed to its remote checkpoint. Agent reports 102 unit/env and 7 real PostgreSQL tests, four mutants, types/build/lint/Knip; root and independent review found no remaining actionable issue                                                    | Canonical integration, PR and complete operational restore qualification remain                                                                                                                                   |
| Telemetry qualification            | Checkpoint `ec1c2e03c` adds native API and external-lookalike probes; image web/worker telemetry checks passed before Registry fixture gap                                                                                                                                                                                          | Complete Registry fixture and full capture/restore run; browser/native-child-process coverage not yet claimed                                                                                                     |

| Subprocessor inventory | Reviewed configuration-manifest mechanism is preserved in combined `3c1a4e1b8`; integrated onto main in dedicated worktree through `598e180de`. Initial generator guard and 111 estate tests pass | Root installed dependencies and requested final Node24 formatting/Knip/estate+CI checks, plus corrected Netlify managed static-client inventory metadata. This remains a configuration review gate, not a Terraform semantic parser or live provider qualification |

| Shared PostgreSQL migration PR #1740 | Head `ba1074dd294061f431419ca9cb28e0bf46ae3ca4`; thin Studio wrapper, complete canonical administrative/evidence/atomicity/quiescence protections, and unchanged numbered migration tree. 263 Studio/Registry-isolation tests,33 shared tests,10 shared PostgreSQL admission tests,25 workspace typecheck tasks,lint/Knip and lane checks passed; independent review clear | Stacked on KMS; GitHub code/security reviews running; canonical parent integration and main CI remain |
| Registry client | Commits `06550df90e80d1e0d26adab111c882a02f479860` and `2eb62d8d291eb83d0e462cc3da9ce5678bd1b18b` pushed to `checkpoint/studio-1243-registry-client`. 21 client tests,79 client/exchange tests,one real service round trip,types/build/lint/Knip/lane checks and three mutants passed; independent review clear | Standalone Registry integration is underway; instance publication records, account linking and import/publish flow still remain |

## Active implementation ownership

Three subagents run independent work in persistent worktrees: `registry-integration` combines the preserved standalone Registry with canonical shared migration/KMS protections; `inventory-integration` completes current-main inventory checks; `telemetry` completes Registry-aware image capture/restore and broader egress observation. The lead owns PII/KMS delivery, integration, reviews and the ledger. Sol handles the larger integration and Luna the bounded inventory/qualification work. Completed independent review slots are reassigned to useful bounded work.

A separate read-only safety patch of the telemetry fixture WIP is retained in the persistent evidence directory; it does not replace an agent-owned Git checkpoint.

## Remote recovery references

The `checkpoint/studio-1243-*` branches preserve combined, recovery, telemetry, alert preferences, alert retry and KMS work. Deployment and PII PR branches preserve their reviewed heads. These references must be updated as further work is committed. The combined branch also retains the New Relic sender/usage-reader implementation; it has not yet completed canonical integration or live validation.

## Completion still required

Canonical integration of encryption, Registry, shared PostgreSQL administration, alerts and recovery must pass current-head reviews and checks. Self-host qualification requires signed distributable images, authenticated restore including keys and objects, safe restart, predecessor upgrade and realistic recovery timing. Managed hosting requires actual provider access, provisioning, backups, deployment/rollback, DNS/TLS and verified costs within the authorized $100 per month hosting budget. Observability requires working collectors, independent retention, budget controls, alert routing and demonstrated delivery. Infrastructure estimates or unit tests alone do not satisfy these operational criteria.

Temporary directories were removed during the local interruption; the removal mechanism has not been established. Shared Git commits survived, while some uncommitted changes and commits in independent temporary clones required reconstruction. Old test claims are retained only as historical context; reconstructed changes receive fresh verification.
