# Message runtime PR 1877 review evidence

Source commit: `d5199d1a5e4b3a52f4df0f1c6f0365612e0949ee`

Schema fingerprint: `6247a584c0624edb2e9a6b98cc3a038859a185c9188f4ad7263759036e4b94da`

Migrations `0001` through `0014` were compared by SHA-256 before and after regenerating unpublished migration `0015`; the comparison was empty.

| Finding                                | Implementation                                                                                                                                                                                                                            | Oracle                                                                                                                                             |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Scheduled participant route            | `message-delivery.ts` constructs `/enter/${token}`.                                                                                                                                                                                       | `message-delivery.test.ts` requires `/enter/` and rejects `/interview/`.                                                                           |
| Contact-less queue starvation          | Enqueue atomically transitions an occurrence with a missing required contact to `blocked` and records its audit event.                                                                                                                    | The real-PostgreSQL queue test proves the older contact-less occurrence blocks and the later eligible occurrence dispatches.                       |
| Populated 0015 upgrade                 | Migration `0015` permanently fails only pending legacy deliveries, clears their leases, preserves terminal evidence, and permits the all-null legacy rendered envelope. A sidecar trigger requires a complete envelope for every new row. | The migration suite applies `0001`–`0014`, inserts a pending legacy delivery, applies `0015`, and checks its exact disposition and current schema. |
| Twilio public callback URL             | The route reconstructs the signed URL from configured `PUBLIC_URL` and the request path.                                                                                                                                                  | The callback test signs the public HTTPS URL while the in-process request uses its local URL.                                                      |
| Postmark callback gate                 | Scheduled Postmark delivery is enabled only when the Postmark webhook credential is configured; SMTP remains independently available.                                                                                                     | A pure startup-selection oracle covers missing/present Postmark callback credentials and SMTP.                                                     |
| Bounded callback request bodies        | Both provider routes reject oversized declared lengths and use the shared capped stream reader before decoding.                                                                                                                           | The callback test covers both an oversized `Content-Length` and a two-chunk stream crossing 16 KiB.                                                |
| Closed-wave link issuance              | The locked issuance target requires no close time or a close time after `statement_timestamp()`.                                                                                                                                          | The link test advances the close time into the past and requires `CONFLICT`.                                                                       |
| Withdrawn-consent link issuance        | The same locked target excludes any withdrawn participant consent.                                                                                                                                                                        | The link test inserts published consent plus a withdrawn participant record and requires `CONFLICT`.                                               |
| Permanent bounce suppression           | Postmark Bounce requires `Inactive`; only `Inactive: true` and spam complaints create a global opt-out.                                                                                                                                   | The callback test proves a soft bounce persists status evidence without creating an opt-out.                                                       |
| Rendered SMS bound                     | The production schedule resolver blocks an occurrence when the rendered SMS exceeds 1,600 characters, before creating delivery work.                                                                                                      | The invalid-render test expands a valid source template past the sender bound and requires a blocked occurrence with no send.                      |
| Rendered-message key rotation          | Rotation has a bounded `message-deliveries` phase, audited read, maintenance-only authenticated rewrap, and immutable non-ciphertext identity.                                                                                            | The rotation suite proves old-key ciphertext changes to the current key and records both maintenance audit events.                                 |
| Required interview link                | The production schedule resolver requires `{{interviewLink}}` in every selected prompt body before rendering and enqueue.                                                                                                                 | The invalid-render test uses a published prompt without the token and requires a blocked occurrence.                                               |
| Interview-link token key guard         | `interview_links.token_key_id` uses the verified integration-key reference trigger.                                                                                                                                                       | The schema suite rejects a complete token envelope naming an unverified key.                                                                       |
| Recipient-scoped opt-out serialization | Callback insertion and final provider handoff both acquire the same channel, blind-index-key, recipient-index advisory transaction lock after participant authority.                                                                      | Two real PostgreSQL transactions prove the same recipient cannot acquire concurrently while a distinct recipient can.                              |
| Rotation audit attribution             | Link rotation passes an explicit rotation authority and emits `interview.link.rotation_read` as `Encryption maintenance`.                                                                                                                 | The rotation suite asserts all link and rendered-message rotation event names and actor labels.                                                    |

Validation on the committed source:

- 47 focused message delivery, encryption rotation, and migration tests passed against PostgreSQL 18.
- 137 schedule schema, migration artifact, and audit catalogue tests passed.
- Studio server typecheck, changed-file formatting and ordinary lint, and repository Knip passed.
- A temporary route mutant replacing `/enter/` with `/interview/` failed at the exact participant-link assertion; the source was restored before commit.
